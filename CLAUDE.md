# The Village App — Claude Context

## What This Is
A maternal health platform for expecting and postpartum moms. 4 verticals built sequentially:
- **V1 Specialists** (🩺) — Find & book OB/GYN, Doula, Midwife, Lactation, Pediatrician, Sleep Coach, Pelvic Floor PT, Perinatal Dietitian, PPD Therapist
- **V2 Milk Connect** (🤱) — Peer breast milk donor marketplace
- **V3 Community** (💬) — Live moderated chat rooms by pregnancy stage
- **V4 Gear + Home** (🛒/🏠) — Gear marketplace, events, brand perks, milestone tracker

## Navigation (6 tabs — v2)
| Tab | Icon | Content |
|---|---|---|
| Home | 🏠 | Milestone card, quick access grid, baby snapshot, events, perks |
| Milk | 🤱 | V2 — Milk Connect donor marketplace |
| Experts | 🩺 | V1 — Specialist directory & booking |
| Connect | 💬 | AI chat + Community rooms + Events (3-way toggle) |
| Gear | 🛒 | V4 — Baby gear marketplace (CPSC-checked) |
| Me | 👤 | Profile, baby card, settings, crisis resources |

## Full Plan
See `docs/MASTER_PLAN.md` — contains all DB schemas, API routes, screens, AI skills, and build sequences for all 4 verticals.

## UI Reference
See `/Users/gp/Desktop/The Village/the-village-app-v2.html` — open in browser. This is the source of truth for all UI, colors, navigation, and copy. **v2 is canonical — supersedes any prior prototype.**

## Operations runbook (read first for any setup / smoke-test / "where do I drop this key" question)

**`docs/OPS_RUNBOOK.md`** is the single source of truth for: account inventory, every required env var per environment (mobile .env + Supabase Edge Function Secrets + GitHub Actions + EAS), per-feature smoke test recipes, rotation calendar (Apple JWT expires ~2026-11-16!), native iOS build cycle, and the "where do I drop X?" lookup. **Update it whenever you add a new env var or smoke test** — that's how it stays usable.

## Source Documents (planning & specs)
**Canonical product intent lives in `docs/source/` as plain markdown (extracted from the original PDFs). Read these directly with the Read tool — MUST be consulted before any major planning, scoping, or implementation decision.** CLAUDE.md summarizes but does not replace them. If a doc conflicts with code, flag it — don't silently pick one.

| # | Document | Path | When to read |
|---|---|---|---|
| 1 | Tech Spec | `docs/source/Village_App_Tech_Spec.md` | Any architecture or integration decision |
| 2 | Feature Specs (PRD v1.1) | `docs/source/Village_Feature_Specs.md` | Any new feature — full per-vertical requirements |
| 3 | Product Roadmap | `docs/source/Village_Product_Roadmap.md` | Sequencing / prioritization calls |
| 4 | Tech Stack & Team Plan | `docs/source/Village_Tech_Stack_and_Team_Plan.md` | Choosing libraries, staffing decisions |
| 5 | MilkConnect Tool Stack | `docs/source/Village_MilkConnect_ToolStack.md` | Any V2 change (Stripe Connect, Twilio, matching, etc.) |
| 6 | GearSwap Tool Stack | `docs/source/Village_GearSwap_ToolStack.md` | Any V4 change |
| 7 | **Risk & Compliance** | `docs/source/Village_Risk_and_Compliance.md` | **MANDATORY before ANY Milk or Gear change** — platform-vs-supplier distinction, Section 230, attorney-review flags |
| 8 | Competitive Analysis | `docs/source/Village_Competitive_Analysis.md` | Positioning, differentiation |
| 9 | Onboarding UX + Copy | `docs/source/Village_Onboarding_UX.md` | Any onboarding screen change |
| 10 | Miami Specialist Directory | `docs/source/Miami_Specialist_Directory.md` | V1 seed data |
| 11 | UI Prototype v2 (canonical) | `/Users/gp/Desktop/The Village/the-village-app-v2.html` | Source of truth for UI, colors, nav, copy |
| 12 | UI Screenshots | `/Users/gp/Desktop/The Village/Screenshot 2026-04-18 at 9.{34.20,52.39} AM.png` | Visual reference |

Originals (PDF + raw TXT blobs) remain in `/Users/gp/Desktop/The Village/` as the editable source. The `docs/source/*.md` files are regenerable from them via `pdftotext -layout` (poppler is installed). Do not hand-edit the `.md` copies — edit the PDF source and re-extract.

## Tech Stack
- **Mobile**: React Native + Expo (managed workflow)
- **Backend**: Supabase-first — RLS handles auth + CRUD directly from mobile. Edge Functions only for AI routes + webhooks.
- **Database**: Supabase PostgreSQL
- **Auth**: Supabase Auth
- **Storage**: Supabase Storage
- **AI**: Claude API — `claude-haiku-4-5` for real-time, `claude-sonnet-4-6` for summaries/cron
- **Maps**: Google Maps + Google Places
- **Payments**: Stripe + Stripe Connect
- **SMS**: Twilio
- **Notifications**: OneSignal
- **Booking**: Calendly (v1), Zocdoc (v2)

## Design Tokens — Brand Kit v2 (villie · May 2026)
**CANONICAL**: see `memory/project_brand_kit_v2.md` for the complete kit (palette · typography · italic rule · mapping cheat sheet).
**Source asset**: `/Users/gp/Downloads/the-village-ig/project/The Village - Brand Kit.html` + the canonical wordmark + V-mark PNGs at `assets/villie-wordmark-v2.png` / `villie-v-mark-v2.png`.

The mobile app's `apps/mobile/src/utils/constants.ts` exposes both v1 legacy names (cream/coco/rust/bark) and v2 canonical names (v2_cream/v2_cinnamon/v2_cocoa/v2_caramel/etc.). As of 2026-05-15 the v1 aliases have been **rerouted to v2 hex values**, so every screen that uses legacy names auto-renders v2 colors. New code should use the v2 names directly.

### Quick palette (v2)
```
--cream:     #F4ECD8  page surface (the 60% in 60·30·10)
--paper:     #FDFBF6  alt page
--parchment: #EAE0C8  card bg
--card:      #FEFAF6  pure-white substitute (no #FFFFFF in app)
--butter:    #FAD080  honey halos, hero gradients
--marigold:  #F2C130  IG hero pop (rare in app)
--cinnamon:  #C07840  ACTION — every CTA, link, active tab. One spark/screen.
--caramel:   #D4A880  italic-name accent, "second moment" fallback
--blush:     #F5BEB6  empathy
--salmon:    #EDA8A0  Feel chapter pill
--cocoa:     #3D1F0E  UI ink (no #000)
--walnut:    #7A4A28  body text on cream
--amber:     #A77349  eyebrows, secondary copy
--sage:      #D8CEB0  cool exhale (reserve)
--moss:      #606E46  Heal chapter pill ONLY
--persimmon: #E0543B  IG ONLY — NEVER in the app
```

### Type system (v2)
- **Wordmark**: hand-drawn `villie-wordmark-v2.png`. Fallback typeset: Caprasimo 400, inline body wordmark mentions only.
- **Display default**: Playfair Display **roman 700** (not italic — "less italic, more presence").
- **Display flourish**: Playfair italic 600 — **one per screen**, on the user's name OR a key noun OR the wordmark dot.
- **Big numbers**: Playfair 800.
- **Body / UI**: Plus Jakarta Sans 400/500/600 (replaces DM Sans + Inter).
- **Metadata / eyebrows**: JetBrains Mono 500, tracking 0.26em, uppercase, amber color.

### Rules of thumb
- **Italic per screen ≤ 1 phrase**. Never on body sentences, links, multi-line paragraphs.
- **Cinnamon per screen = 1 spark**. CTA, italic word, OR a number. Reach twice → second is amber/caramel.
- **60·30·10**: 60% cream/paper, 30% earth (caramel/butter/parchment/blush), 10% spark (cinnamon).
- **Chapter colors stay in chapters** (Manual sub-palette).
- **Black** → use cocoa. **Pure white** → use card. **Persimmon** → IG only.
- **Wordmark always lowercase** — `villie`, never "Villie".

## Architecture Rules
1. **Supabase direct** for all CRUD — never write Express routes for reads/writes
2. **Edge Functions** only for: AI skill routes, Stripe webhooks, Twilio SMS, Calendly webhooks
3. **RLS on every table** — never rely on application-level auth checks alone
4. **One migration file per vertical** — `001_v1.sql`, `002_v2.sql`, etc. Migration filenames must be numeric-prefix-only (no `001b` — CLI skips them silently).
5. **`supabase gen types typescript`** for all TypeScript types — never write them manually. Generated file: `packages/shared/src/types/supabase.ts`. API interfaces in `apps/mobile/src/api/*.ts` currently drift from generated types (hand-written). **Migration in progress (incremental):** `apps/mobile/src/lib/supabase.ts` exports both `supabase` (default — untyped, used by un-migrated files) and `supabaseTyped` (same runtime instance, generic-parameterized as `Database`) plus a `Tables<'name'>` helper. Per-file migrations: replace `import { supabase }` with `import { supabaseTyped as supabase }`, then refactor that file's hand-written interfaces to derive from `Tables<'<table>'>` / `Database['public']['Tables'][X]['Insert']`. Each file can be migrated independently — `tsc` errors stay scoped. Regenerate generated types after schema changes via `pnpm --filter shared supabase:types`.
6. **Prompt caching** on all Claude system prompts — `cache_control: { type: "ephemeral" }`
7. **Haiku for real-time** (message scan, Q&A), **Sonnet for batch** (summaries, weekly cron)

## Naming Conventions
- Files: `kebab-case.ts`
- Components: `PascalCase.tsx`
- DB tables: `snake_case`, prefixed by vertical (`milk_`, `gear_`, `room_`)
- Edge Functions: `kebab-case` folder name

## Build Order
V1 fully live → V2 → V3 → V4+Home. Never start next vertical until current is in production.

## Current Status
🟢 **V5 Phase 5.1 shipped** (2026-05-29). OTA `c62ac3cb` published. Mom hero card on Home (taps into MomHubScreen) + Manual/Playbook toggle replacing the retired Mom/Baby audience switch. Playbook view is a coming-soon teaser ("A book that grows with you.") until 5.2 wires Baby Check-in data and 5.3 ships Pro paywall. See `docs/V5_GROW_WITH_YOU.md` for the full 3-phase plan + pricing decisions.

🟢 **Build 11 in Apple Beta Review · Build 12 prep landed** (2026-05-29). Build 11 awaiting external-tester approval. Build 12 prep already on main: (1) Manual PDF export wired via expo-print + expo-sharing (native-safe try/catch downgrades to "coming soon" on Build 11, lights up automatically on Build 12); (2) 3 stale Info.plist permissions removed (NSContacts, NSMicrophone, NSLocationAlways) to dodge Apple Review permission-mismatch rejection; (3) Sentry source-map auto-upload re-enabled in eas.json — needs `SENTRY_AUTH_TOKEN` in EAS env before `eas build` per `docs/BUILD_12_RUNBOOK.md`. 2 of 5 external testers signed in (Alana, Giulianna); Cindy account deleted for fresh-signup recovery; Daniela + Sophia not yet started.

🟢 **V4 Phases G1+G2+G3+G4 complete** (as of 2026-04-22). See V4 Gear + Home build table below for deliverables. G5–G8 pending.

🟢 **Agents runtime integration — internal bridge landed** (as of 2026-04-22). Starter agent runtime (external HTTP service at `AGENT_BASE_URL` with `/health`, `/triage`, `/run`) connected via: (1) shared contracts in `packages/agents-client` (`schemas.ts` / `client.ts` / `mapper.ts` — `AgentRequest`, `AgentResponse`, `IntegrationEnvelope`, `VILLAGE_WORKSTREAMS`, `toUiCard`; client is invoker-agnostic so mobile + edge can share types without pulling supabase-js into the package); (2) 3 Supabase Edge Functions — `agents-health`, `agents-triage`, `agents-run` — as the **only** layer that knows `AGENT_BASE_URL` (plus optional `AGENTS_BRIDGE_SECRET` forwarded as `x-agents-secret`); (3) mobile wrapper `apps/mobile/src/api/agents.ts` binds the shared client to `supabase.functions.invoke`; (4) `InternalAgentsScreen` (`apps/mobile/src/screens/internal/`) — hidden modal rendered in RootNavigator only when `EXPO_PUBLIC_INTERNAL_AGENTS_ENABLED === '1'`, launched via a small `AGT` badge (top-right), shows full envelope, flags `requires_human_review`, flags workstream drift vs. Village taxonomy. **Hard constraints**: no public user flow calls agents; runtime outputs never mutate DB state; mobile never learns `AGENT_BASE_URL`. See `/Users/gp/Desktop/the-village-claude-handoff/` for the canonical contract, test plan, and acceptance checklist.

🟢 **V4 Phase G1 complete** (as of 2026-04-22) — Home foundation: `baby_profiles` + `milestone_library` migrations + 52-week content seed + RLS + RPCs (`get_milestones_for_week`, `get_my_current_milestone`), home API + Zustand store, BabyProfileSetupScreen (DOB/preemie/gender/feeding), HomeScreen (greeting + HeroMilestoneCard + BabySnapshotCard + timeline link), MilestoneDetailScreen, MilestoneTimelineScreen (W1–W52 horizontal rail), HomeNavigator wired into Home tab. The global Villie FAB continues to overlay. **Migrations not yet applied locally** (Docker Desktop was stopped at build time) — run `supabase db reset` when Docker is up. No AI yet in G1; milestone AI summaries land in G7 cron.

🟢 **V3 Phases C2 + C4 + C5 code complete · Connect tab still hidden** (as of 2026-04-24) — C5 adds migration 028 (icebreaker_suggestions + room_match_suggestions + room_weekly_summaries + ai_companion_mentions + `has_village_mention` helper + weekly-summary pg_cron), 4 edge functions (`room-ai-companion` Haiku 0.4 w/ rate-limit + crisis suppression, `room-icebreaker` Haiku 0.8 one-sentence opener, `room-auto-match` Haiku 0.2 never-support-as-primary, `room-weekly-summary` Sonnet batch anonymized digest + OneSignal fan-out), community API extensions, and RoomChatScreen wiring (icebreaker chip above composer, @village detection fires companion after C4 clear, ai_companion bubble w/ gold accent + system digest card). Connect tab still hidden per standing rule — flip with caution once beta list is ready.

🟢 **V3 Phase C2 + C4 code complete · Connect tab still hidden** (as of 2026-04-23) — Room messaging + AI safety pipeline code are both live. C4 additions: migration 027 (flipped `ai_scan_status` default back to `'pending'`, fixed `crisis_flags.message_id` FK, `list_room_messages` filters to `'clear'` only, AFTER-INSERT trigger → pg_net POST to edge function, 3 moderator RPCs), edge function `room-message-scan/index.ts` (Haiku 4.5 crisis + moderation in Promise.all with 3s timeout, fail-open to `'clear'`, crisis flag insert + Twilio SMS to moderators), `CrisisResourcesSheet` component, `RoomChatScreen` wired with "Scanning…" pill + verdict polling + crisis-route to sheet. **Connect tab remains hidden in `AppNavigator.tsx`** per user rule — do not re-enable without explicit ask. Re-enabling requires the pre-launch gates listed in the C4 row below (env + Postgres GUCs + Twilio + ≥1 active moderator per room + crisis drill). ModeratorDashboardScreen still pending (deferred — the three RPCs are already live). The global Villie AI help chat continues to overlay all authenticated screens.

🟢 **V3 Phase C1 complete** (archived reference) — V3 Community Rooms foundation: DB migration (006) with 10 tables + triggers + RPCs, RLS migration (007) with **USING(FALSE) verified** on `user_anonymous_identities` and `crisis_flags` (security boundary tested end-to-end), 4 seed rooms + PPD crisis resources, community API client, CommunityHomeScreen (stage-matched room discovery), CommunityNavigator. C2–C7 deferred.

## V2 Milk Connect — Build Progress
| Phase | Status | Notes |
|---|---|---|
| M1 | ✅ Done | DB migration (004, 005), milk API + Zustand store, milk-safety-screener + milk-questionnaire-coach + milk-stripe-connect Edge Functions, MilkConnectHomeScreen, BecomeDonorIntroScreen, DonorQuestionnaireScreen (12 Qs + AI coach), TrustBadgeBuilderScreen (diet flags + medications modal), CreateListingScreen, StripeOnboardingScreen (WebView), OnboardingCompleteScreen, MilkNavigator wired into AppNavigator |
| M2 | ✅ Done | migration 017 (rating_avg trigger, narrative cache, extended search_donors_near RPC), milk-trust-narrative + milk-donor-qa Edge Functions, DonorSearchListScreen (FlashList + filter drawer + name search), DonorMapScreen (badge-colored pins + callout), DonorProfileScreen (hero, AI narrative, trust badge card, pricing, diet/meds, reviews, AI Q&A modal, sticky action bar), SavedDonorsScreen, DonorCard + FilterDrawerModal components, MilkNavigator updated with all M2 screens |
| M3 | ⚠️ Deprecated · cash-only since 2026-05-21 | **Decision 2026-05-21**: V2 Milk Hub flipped to **cash-only MVP** (same posture as V4 Gear — see memory/project_milk_cash_only.md). Stripe Connect path stays in tree behind `EXPO_PUBLIC_MILK_STRIPE_ENABLED` (default OFF) for future re-enable; no production code reaches it. Active cash flow: DonorProfile → SafeMilkHandoffModal → getOrCreateThread → MilkMessageDetail. Below is the legacy Stripe scope, marked "as built" so the historical record stays clean. **Legacy (deprecated)**: migration 018 (transaction lifecycle cols, supply-decrement trigger, list_my_orders + get_transaction_pickup_address RPCs, donor address_line/phone), milk-match-donors + milk-purchase-intent + milk-purchase-confirmed Edge Functions (Stripe Connect destination charge + 15% platform fee + Twilio SMS to both parties), milk API (createPurchaseIntent, confirmPurchase, etc.), MilkMatchScreen, MilkPurchaseScreen (Stripe PaymentSheet), MilkOrderConfirmScreen, MilkNavigator routes. **Active**: milk-match-donors stays in use (AI ranking, no payment); SafeMilkHandoffModal added 2026-05-21 with cold-chain + cash/P2P-only copy; the dispute + shipping label paths in M5 are de-facto dormant in cash-only mode (no transactions = no disputes to file). |
| M4 | ✅ Done | migration 019 (bump_thread_last_message trigger, list_my_milk_threads + mark_thread_read + list_reviewable_orders RPCs, message ordering indexes), milk API extended (listMyMilkThreads, getThreadMessages, sendMilkMessage, markThreadRead, getOrCreateThread, listReviewableOrders, submitMilkReview), MilkMessageThreadsScreen (inbox with avatars + unread dots + recipient/donor side detection), MilkMessageDetailScreen (chat bubbles + optimistic send + 30s poll + KeyboardAvoidingView), MilkOrdersScreen (order history + status pills + leave-review CTA), MilkReviewSubmitScreen (5-star selector + body input — fires existing rating_avg trigger), DonorProfile + OrderConfirm Message buttons now create thread via getOrCreateThread, Home header gets Inbox + Orders icon buttons |
| M5 | ✅ Done | migration 020 (milk_disputes + milk_shipping_labels + milk_legal_acceptances + milk_analytics_events tables, RLS, mark_transaction_disputed trigger, get_dispute_for_transaction RPC), milk-dispute-open + milk-shippo-label Edge Functions (Stripe Connect-aware, Shippo PDF labels, Twilio SMS to both parties), milk API extended (openDispute, getDisputeForTransaction, buyShippoLabel, getShippingLabel, hasAcceptedLegal, recordLegalAcceptance, LEGAL_DOC_VERSION registry), LegalDisclosureModal (3-step informed consent per Risk & Compliance §3.2 — scroll-gated disclosure, three explicit acknowledgments, version-pinned persistence), MilkDisputeOpenScreen (role-aware reason codes, 20-char min description, what-happens-next note), MilkShippingLabelScreen (pre-filled donor/recipient addresses, Shippo rate fetch + label PDF + tracking URL, idempotent), useAnalytics extended with milk events + server persistence for compliance-relevant events (milk_analytics_events), MilkPurchaseScreen gated by LegalDisclosureModal before payment + purchase event tracking, MilkOrdersScreen Report-issue CTA on paid/fulfilled/disputed orders, MilkNavigator wired with M5 routes |

## V3 Community — Build Progress
| Phase | Status | Notes |
|---|---|---|
| C1 | ✅ Done | migration 006 (rooms, room_members, user_anonymous_identities, room_messages, room_message_reactions, room_moderators, pinned_resources, crisis_flags, room_events, room_presence + bump_room_member_count trigger + list_rooms_for_discovery/join_room/leave_room RPCs + 4 seed rooms + 3 PPD pinned crisis hotlines), migration 007 RLS (**USING(FALSE) on user_anonymous_identities + crisis_flags verified end-to-end via `SET ROLE authenticated`**, rooms/room_moderators/pinned_resources/room_events public read, room_members/room_presence/reactions own-only, messages scoped to cleared+member), community API (listRoomsForDiscovery, joinRoom, leaveRoom, getMyMembership, getRoomBySlug, listPinnedResources), CommunityHomeScreen (stage-match ranking, Join&open / Leave CTAs, anonymous-mode badge, accessibility labels), RoomChatScreen stub with pinned-resources bar (always-visible crisis hotlines), CommunityNavigator wired into Connect tab |
| C2 | ✅ Done | migration 026 (flipped `room_messages.ai_scan_status` default `pending`→`clear` as C4 placeholder — C4 will reverse + add BEFORE-INSERT AI scan pipeline; `list_room_messages(p_room_id, p_limit, p_before)` SECURITY DEFINER RPC with membership guard, LEFT JOIN auth.users for sender_name/avatar_url + JSONB reactions aggregate via subquery jsonb_object_agg; added `room_messages` + `room_message_reactions` to `supabase_realtime` publication (guarded DO block so re-runs are safe); `bump_sender_last_read` AFTER-INSERT trigger so a user's own send doesn't count as unread on reopen; `mark_room_read(p_room_id)` SECURITY INVOKER RPC). community API extended (`REACTION_EMOJIS` ❤️🤗💪😂😢🙏, `listRoomMessages` with cursor pagination, `sendRoomMessage` with 1-2000 char validation, `markRoomRead`, `subscribeToRoomMessages` returning unsubscribe fn w/ server-side `room_id=eq` filter + client-side `ai_scan_status IN (clear, pending)` belt-and-braces, `addReaction`/`removeReaction` with dedupe-error tolerance, `listMyReactions` for current-user emoji set). RoomChatScreen fully rewritten (~460 lines): initial load via `Promise.all([getRoomBySlug, listPinnedResources, listRoomMessages, listMyReactions])`; Realtime subscription w/ optimistic-id collision skip + lazy refetch via `listRoomMessages(limit:1)` for non-self messages to enrich sender_name/avatar; optimistic send pattern (temp-id bubble → real-id swap on insert response); MessageBubble component (mine/theirs distinct border-radius styling, sender name + avatar + relative timestamp); long-press → reactionTarget modal with 6-emoji picker; `toggleReaction` with optimistic flip + revert-on-error; `useFocusEffect(markRoomRead)` on screen focus; pinned resources bar always visible above feed w/ `tel:` + URL Linking (crisis hotlines never scroll away); KeyboardAvoidingView composer with 2000-char TextInput + send button. **Connect tab temporarily restored then re-hidden** in `AppNavigator.tsx` (2026-04-23 — user decided to keep it hidden until C4 AI safety ships). TABS back to 5: Home/Milk/Experts/Gear/Me. C2 code preserved in repo. Also 2026-04-23: fixed migration 023 (`DROP FUNCTION IF EXISTS get_gear_listing(UUID)` before the CREATE OR REPLACE since the return signature changed — reset was erroring SQLSTATE 42P13). Migrations 001–026 apply cleanly from `supabase db reset`. Typecheck + lint clean. |
| C3 | ⚪ Pending | Anonymous mode: alias generation Edge Function, AnonymousOnboardingScreen, AnonBubble |
| C4 | ✅ Done (code) | migration 027 — flipped `room_messages.ai_scan_status` DEFAULT back to `'pending'` (reverses C2 placeholder); fixed `crisis_flags.message_id` FK pointed at V1 `messages` (migration 006 typo) → now `room_messages(id) ON DELETE CASCADE`; `list_room_messages` RPC now filters `ai_scan_status = 'clear'` only (drops C2 pending fallthrough); `scan_room_message_async()` AFTER-INSERT trigger on `room_messages` — trusted message_types {system, ai_companion, expert} marked `'clear'` inline, user messages fire `net.http_post` to `/functions/v1/room-message-scan` wrapped in BEGIN/EXCEPTION WHEN OTHERS THEN NULL so insert never fails when pg_net or settings are missing; moderator RPCs `is_moderator_anywhere()`, `list_open_crisis_flags_for_moderator()` (severity-sorted critical→high→medium→low, joins rooms + room_moderators + room_messages + auth.users for sender_name), `resolve_crisis_flag(flag_id, action, notes)` (gated on room_moderators.is_active, action ∈ {reviewed, escalated, resolved}, sets resolved_at only when resolved). Edge Function `room-message-scan/index.ts` — service-role Supabase client + Anthropic SDK (Haiku 4.5 `claude-haiku-4-5-20251001`), two hardcoded prompts with `cache_control: 'ephemeral'`: CRISIS_PROMPT (is_crisis + severity critical/high/medium/low/none + phrases + assessment JSON) and MOD_PROMPT (should_flag + reason JSON), runs both in `Promise.all([withTimeout(3000)])` with `.catch(()=>null)`, verdict precedence crisis > flagged > clear, PATCHes `room_messages.{ai_scan_status, ai_scan_at}`, on crisis inserts into `crisis_flags` (severity 'none'|'low' bumped to 'high' to satisfy CHECK) + `notifyModerators` fetches phones from `users` table and fan-outs to `twilio-sms` edge function with 80-char snippet. **Fail-open policy**: any timeout/parse/network error → row PATCHed to 'clear' so chat stays usable; a future nightly sweep can re-scan cleared rows for drift. community API extended: `subscribeToRoomMessages` now listens for both INSERT and UPDATE `postgres_changes` and only emits when `ai_scan_status === 'clear'` (handles both immediate trusted inserts and the async PATCH-to-clear); `waitForScanVerdict(messageId, {intervalMs=1500, maxAttempts=5})` polls the row until status leaves 'pending' (~7.5s budget); moderator types + RPCs `isModeratorAnywhere`, `listOpenCrisisFlags`, `resolveCrisisFlag`. Component `CrisisResourcesSheet` — modal bottom sheet with 4 resources (988, Crisis Text Line 741741, PSI 1-800-944-4773, 911), iOS uses `sms:XXX&body=…` / Android `sms:XXX?body=…`, silent catch on Linking failure, accessibility-labeled TouchableOpacity cards with colored left borders, optional `lead` prop for crisis-routing context. RoomChatScreen wired: optimistic own bubble now starts `ai_scan_status: 'pending'`, MessageBubble renders a "Scanning…" pill (ActivityIndicator + text, 75% opacity bubble) on own pending messages, post-send `waitForScanVerdict` background poll handles verdict — crisis removes bubble + opens `<CrisisResourcesSheet>` with crisis-specific lead copy, flagged removes bubble + silent `Alert.alert('Message held for review')`, clear flips local status. **Deferred to C5 gate**: ModeratorDashboardScreen (uses the three moderator RPCs now live). Typecheck + lint clean. Migrations 001–027 apply cleanly from `supabase db reset`. **Pre-launch gates for enabling Connect tab**: (1) `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` + `ANTHROPIC_API_KEY` set for edge function; (2) Postgres GUCs `app.supabase_url` + `app.service_role_key` set via `ALTER DATABASE` so pg_net can POST; (3) `twilio-sms` function live (reused from V1); (4) at least one user has `room_moderators.is_active=TRUE` per moderated room; (5) crisis drill + C7 audit before exposing to users. |
| C5 | ✅ Done (code) | migration 028 — `icebreaker_suggestions` (UNIQUE(user_id, room_id), used_at/dismissed_at, own-read + own-update RLS, `get_icebreaker(p_room_id)` + `dismiss_icebreaker(p_room_id)` RPCs), `room_match_suggestions` (UNIQUE(user_id), primary_room_id + secondary_room_ids UUID[] + reason, own-read RLS, `get_my_room_match()` SECURITY DEFINER returning hydrated JSONB), `room_weekly_summaries` (UNIQUE(room_id, period_start), 800-char CHECK, members-only SELECT RLS), `ai_companion_mentions` audit/rate-limit ledger (own-read RLS), `has_village_mention(body)` IMMUTABLE helper w/ word-boundary regex `(^|[^a-z0-9_])@village($|[^a-z0-9_])`, pg_cron `room-weekly-summaries-sunday` at Sunday 11:00 UTC (06:00 ET) gated on pg_extension existence. 4 new Edge Functions: `room-ai-companion` (Haiku 4.5 temp 0.4, pulls last 20 cleared messages + asker stage, rate-limit 3/user/room/hour, suppresses on C4 crisis verdict — never competes with CrisisResourcesSheet routing, inserts reply as `message_type='ai_companion'` with `ai_scan_status='clear'` service-role, logs every invocation to ai_companion_mentions with {crisis_detected, suppressed} flags, fail-silent on AI/insert errors, disclaimer enforced post-hoc via `/medical advice/i` regex), `room-icebreaker` (Haiku 4.5 temp 0.8, one-sentence opener ≤160 chars, no emoji regex guard via `[\p{Emoji_Presentation}\u200d]`, hardcoded FALLBACKS per room_type {stage_local/topic/support}, upserts on (user_id,room_id) conflict), `room-auto-match` (Haiku 4.5 temp 0.2, excludes already-joined rooms, computes current_week from users.due_date, hard rule: support rooms NEVER become primary — coerced to secondary if AI tries, strict slug→id resolution against candidate list to prevent hallucinated slugs, upserts on user_id conflict with generator_version='haiku-4.5-v1'), `room-weekly-summary` (Sonnet 4.6 batch, 7-day cleared user-only messages capped at 400/8KB context, anonymized 3–4 sentence digest — prompt forbids quotes/names/emoji, quiet-week fallback under 5 msgs, inserts `message_type='system'` card with `📮 Weekly digest` prefix, fan-out OneSignal push via existing `push-notify` to members with `notif_pref='all'`, idempotent via UNIQUE(room_id, period_start), fail-soft per room in batch mode). community API extended: `hasVillageMention(body)` mirror of SQL helper, `invokeAiCompanion(messageId)` fire-and-forget, `getIcebreaker(roomId)` / `dismissIcebreaker(roomId)` / `generateIcebreaker(roomId)`, `getMyRoomMatch()` returning hydrated `{primary_room, secondary_rooms[], reason}`, `refreshRoomMatch()` fire-and-forget, `getLatestWeeklySummary(roomId)`, `RoomMatchSuggestion` + `RoomWeeklySummary` types. `CommunityHomeScreen` fires `generateIcebreaker` on first join (silent fail). `RoomChatScreen` wired: loads icebreaker on mount via `getIcebreaker`, renders golden "✨ Icebreaker" chip above composer with tap-to-prefill + "Not now" dismiss (hidden when draft has text), auto-dismisses when sent, detects `@village` mention and fires `invokeAiCompanion` only AFTER C4 scan returns 'clear' (no race with crisis routing). MessageBubble handles 3 types: `ai_companion` renders with gold-accent card + "✨ Villie · AI companion" label + cream background; `system` renders as centered rust-tinted digest card with no reactions/long-press; `user` unchanged. Typecheck + lint clean. Migrations 001–028 apply cleanly. **Deferred**: a CommunityHome "Suggested for you" card over `get_my_room_match` (small UI task — RPC is live), a RoomInfoScreen tab surfacing `getLatestWeeklySummary` as standalone, push-match invocation on profile save (hook into OnboardingProfile / Me screen). **Pre-launch gates**: OneSignal external_ids registered for all room members (V1 infra already handles this via useOneSignal); pg_cron Postgres GUCs `app.supabase_url` + `app.service_role_key` set for summary cron to actually fire on Supabase Pro; Sonnet billing budget headroom (weekly summary × active rooms — 4 seed rooms is trivial, scale with room growth). |
| C6 | ⚪ Pending | Expert events (Calendly webhook), ExpertEventScreen, RSVP, OneSignal push |
| C7 | ⚪ Pending | Load test (100 concurrent), anon-table RLS audit, crisis drill, moderator runbook, legal review, 50-user beta |

## V4 Gear + Home — Build Progress
| Phase | Status | Notes |
|---|---|---|
| G1 | ✅ Done | migration 008 (`baby_profiles` w/ STORED `current_week_number` + preemie corrected_age offset, `user_notifications_feed`, RLS own-only for baby_profiles, service-insert on notif feed), migration 009 (`milestone_library` w/ 52-week seed rotating motor/social/communication/sleep/feeding/sensory/cognitive, `get_milestones_for_week` + `get_my_current_milestone` SECURITY DEFINER RPCs), home API (`getMyBabyProfile`/`upsertBabyProfile`/`getMilestonesForWeek`/`getMyCurrentMilestone`/`listMyNotifications`/`markNotificationRead`) + `formatAge`/`computeWeekNumber` helpers, `useHomeStore` (baby profile + current milestone + fetchAll), HomeScreen (time-of-day greeting, HeroMilestoneCard, BabySnapshotCard, empty-state CTA, "Coming soon" teaser for Events/Perks/Checkin/Gear), BabyProfileSetupScreen (4-step: DOB + preemie+due-date toggle → gender → feeding method → review), MilestoneDetailScreen (hero + category cards + sleep/feed stats + "not a doctor" disclaimer), MilestoneTimelineScreen (horizontal W1–W52 rail + preview + drill-in to detail), HomeNavigator wired into Home tab, replacing V4 placeholder. Global Villie FAB continues to overlay on all Home screens. |
| G2 | ✅ Done | migration 010 (`postgis` extension, `events` + `event_rsvps` tables with type-specific CHECK constraints, auto-waitlist trigger, RLS public-read non-cancelled + own-only rsvps, `list_events_near` RPC with PostGIS ST_DWithin + distance sort, `list_my_rsvps` SECURITY DEFINER RPC, 4-event seed — 2 Miami local + 2 webinars, all flagged `is_partner` or `is_third_party`). events API (`listUpcoming`/`getById`/`rsvp`/`cancelRsvp`/`markCalendarAdded`/`getMyRsvpForEvent`/`listMyRsvps`) + `formatEventWhen`/`formatDistance`/`timeUntilLabel` helpers. `useEventsStore` (upcoming + myRsvps + pastRsvps + fetchers). Screens: EventsListScreen (FlashList + location permission non-blocking + Local/Webinars/All type chips + age-tag chips + distance-sorted), EventDetailScreen (type-branching: local gets "Open in Maps" via `maps.apple.com` URL; webinar gets status-gated "Join stream"; third-party disclaimer rendered when `is_third_party=true`; capacity bar; RSVP/cancel), RsvpConfirmScreen (modal, `expo-calendar` handoff with permission check + writable-calendar detection + persists `calendar_event_id` server-side), MyRsvpsScreen (Upcoming/Past tabs + waitlist chip + calendar-added indicator). HomeNavigator extended with 4 G2 routes. HomeScreen `EventsTeaserCard` replaces the Events chip in the "Coming soon" block and deeplinks to EventsList. Third-party disclaimer copy: "Hosted by an external provider. The Village curates but does not operate this event." |
| G3 | ✅ Done | migration 011 (`brand_deals` + `deal_claims` with `affiliate_network` stub enum `impact|shareasale|cj|direct|none`, CHECK constraints per deal_type, sort_priority + is_partner + FTC `disclosure_required` flags, eligibility_age_tags + eligibility_countries), RLS (public-read active deals, own-only claims, service-role webhook updates), RPCs: `list_perks(p_age_tags, p_country, p_category)` + `claim_perk(p_deal_id)` that generates per-click SubID (`v_<user8>_<deal8>_<rand6>`) and resolves affiliate URL templates via `{subid}` substitution + `list_my_claims`, 4-deal seed (Comotomo discount code, UPPAbaby affiliate link, Bobbie free sample, Nesting Co. partner offer via ShareASale). perks API (`listPerks`/`getPerk`/`claimPerk`/`listMyClaims` + helpers `ctaLabelFor`/`categoryLabel`/`disclosureTextFor`/`claimStatusLabel`). `usePerksStore` (perks + myClaims + fetchers). Screens: PerksListScreen (FlashList + category chips + stage-match toggle derived from `baby_profile.current_week_number`), PerkDetailScreen (hero + long_description + FTC disclosure block + meta rows + terms link + sticky CTA), PerkClaimScreen (modal; branches on `redemption_method` — `show_code` reveals copyable code via `expo-clipboard`; `tap_link` opens affiliate URL; `request_sample` opens brand sample form; shows FTC disclosure), MyClaimsScreen (history + status pill + reopen link). HomeNavigator extended with 4 G3 routes. HomeScreen `PerksTeaserCard` (🎁 "Perks for your stage") added between events card and coming-soon block; "Perks" removed from coming-soon text. Edge Functions: `perks-build-deeplink` (stub — network-specific SubID param mapping: Impact=`subId1`, ShareASale=`afftrack`, CJ=`sid`), `perks-redemption-webhook` (stub — shared-secret gated via `PERKS_WEBHOOK_SECRET` until per-network signature verification is wired). FTC posture: disclosure banner rendered whenever `disclosure_required=true` or `is_partner=true` on both PerkDetail and PerkClaim screens. |
| G4 | ✅ Done | migration 012 (`gear_listings` with ALLOWLIST category CHECK enum — {stroller, carrier_wrap, high_chair, bouncer_swing, toy, feeding_gear, clothing, book, activity_center, nursery_furniture} — no car_seat/breast_pump/sleep_positioner/inclined_sleeper/helmet, CONSTRAINT toy_year_safe ≥ 1978, CONSTRAINT crib_year_safe ≥ 2011, `gear_listing_images` + `gear_saved_listings` with `bump_gear_save_count` trigger, RLS public-read active/pending/sold + owner-read any + service-role override, RPCs: `list_gear_near` with PostGIS ST_DWithin + distance sort, `create_gear_listing` SECURITY INVOKER with `ST_SetSRID(ST_MakePoint,4326)::geography`, `get_gear_listing` with images JSONB + `is_saved` + seller info, `list_my_gear_listings`, `list_my_saved_gear`; 6 Miami seed listings including a free Lovevery play gym). gear API (`listGear`/`getListing`/`createListing`/`updateStatus`/`saveListing`/`unsaveListing`/`listMyListings`/`listMySaved` + helpers `categoryLabel`/`conditionLabel`/`formatPrice`/`requiresYearManufactured`/`SUBCATEGORIES_BY_CATEGORY`/`PROHIBITED_CATEGORIES`). `useGearStore` (feed + myListings + saved + fetchers). Screens: GearBrowseScreen (FlashList + category chips in ListHeader + non-blocking `expo-location` + Saved/Mine header links + FAB "＋ List an item"), GearListingDetailScreen (horizontal image carousel + category + CPSC badge + meta grid + age tags + description + seller card + "Open pickup area in Maps" + safety block "Before you meet" with cpsc.gov/Recalls reminder + save/unsave + Message-seller placeholder noting G6), CreateListingScreen (modal; prohibited-items notice at top, category+subcategory allowlist chips, title/description validation, brand/model, year-manufactured conditionally required with CPSIA/crib copy, condition, age_tags, is_free toggle + dollars input, pickup city/ZIP, `expo-location` + `reverseGeocodeAsync` "Use my location", `expo-image-picker` multi-select up to 5, fetch+arrayBuffer upload to Supabase Storage bucket `gear-listings`, submit via `gearApi.createListing`), MyListingsScreen (status pills + mark sold/withdraw/reactivate), SavedGearScreen (wishlist + unsave + sold/unavailable indicator). GearNavigator (native-stack) wired into Gear tab in AppNavigator, replacing placeholder. Typecheck clean. **Note**: Supabase Storage bucket `gear-listings` must be created (public read) before CreateListingScreen can upload; migrations not yet applied locally. |
| G5 | ✅ Done (code) · ⚠️ Legal sign-off pending | migration 023 — extends `gear_listings` w/ cpsc_recall_status CHECK {clear,recalled,unknown} + cpsc_recall_id + cpsc_recall_url + cpsc_checked_at + upc + vision_confidence (derived is_cpsc_checked mirrors status='clear' for G4 compat); `cpsc_recall_cache` table w/ lowercased brand/product_name + upcs[] GIN index + service-write RLS + public-read; RPC `mark_listing_cpsc` SECURITY INVOKER owner-scoped (auto-withdraws listing + writes removed_reason when status='recalled'); extends user_notifications_feed.type CHECK w/ 'gear_recall'; RPC `sweep_active_listings_for_recalls` SECURITY DEFINER service-role-only — matches by UPC or brand+title LIKE, withdraws + writes seller notification; extends `get_gear_listing` to return all new CPSC fields; pg_cron `gear-cpsc-recall-sync` at 06:00 UTC (02:00 ET). 4 new Edge Functions: `gear-cpsc-check` (cache-first by UPC → live SaferProducts.gov query by RecallTitle + client-side brand filter → upserts cache on hit → owner-scoped mark_listing_cpsc via user JWT when listing_id provided; fail-open as 'unknown' on CPSC timeout/error — nightly sweep catches drift), `gear-vision-identify` (Claude Haiku 4.5 multimodal w/ ephemeral prompt caching, hardcoded ALLOWED_CATEGORIES + PROHIBITED list in prompt, strict JSON out w/ coerce fn clamping to enum + confidence 0-1 + code-fence stripping, fail-soft empty response on error), `gear-upc-lookup` (Go-UPC primary → UPCitemdb fallback waterfall, 5s timeout each, graceful `{found:false, reason:'no_api_keys'}` when unconfigured), `gear-cpsc-recall-sync` (365-day lookback from saferproducts.gov → 500-row chunked upsert → sweep RPC invoke, service-role auth-header gated). Components: `CPSCBadge` ("CPSC Checked ✓" pill — olive green, size={sm,md}, optional onPress), `CPSCRecallBlockModal` (full-screen hard block w/ hazard/remedy card + expandable "Why is this blocked?" explainer referencing CPSIA §19 + "Open CPSC notice →" Linking to recall.url or cpsc.gov/Recalls fallback + single "Got it" close btn — no "list anyway" escape hatch), `BarcodeScannerModal` (expo-camera CameraView w/ onBarcodeScanned debounced via firedRef, accepts upc_a/upc_e/ean13/ean8, 240px reticle w/ 4 corner brackets, permission-gated w/ canAskAgain guard, resets on reopen). CreateListingScreen extended: "Scan barcode" + "Identify from photo" quick-fill row, onBarcodeScanned → gearApi.upcLookup → autofill title/brand + setUpc, identifyFromPhoto → fetch→blob→FileReader base64 → gearApi.visionIdentify → autofill category/subcategory/condition/title/brand w/ confidence% note, **pre-insert CPSC gate** on submit (calls cpscCheck before uploading photos — hard block + CPSCRecallBlockModal render when status='recalled'; allows post when 'clear' or 'unknown'), post-insert fire-and-forget cpscCheck(listing_id) to persist verdict via mark_listing_cpsc. GearListingDetailScreen renders `<CPSCBadge size="sm" />` in badgeRow when listing.cpsc_recall_status === 'clear'. gear API extended (`upcLookup`, `visionIdentify`, `cpscCheck` + types `UpcLookupResult`, `VisionIdentifyResult`, `CpscCheckInput`, `CpscCheckResult`, `CpscRecallSummary`). Typecheck + lint clean. **Legal sign-off gate**: Risk & Compliance §2.1 calls for CPSC API integration + prohibited items policy + DMCA-style takedown process. Implementation matches spec; pre-launch attorney review still required for prohibited-items-policy publication + takedown SOP (Risk & Compliance P2: "CPSC compliance review for Gear Marketplace prohibited items policy $500-1,000 before launch"). Requires: GO_UPC_API_KEY or UPCITEMDB_API_KEY env (optional — both absent degrades to manual entry), SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY for sync, app.supabase_url + app.service_role_key Postgres settings for pg_cron. |
| G6 | ✅ Done | migration 024 (gear_message_threads w/ `safe_meeting_ack_at` + UNIQUE(listing_id, buyer_user_id) + no-self-chat CHECK, gear_messages w/ bump_gear_thread_last_message trigger, gear_listing_reports 8-reason enum + 4-status enum + seller-silent RLS, gear_legal_acceptances version-pinned, gear_analytics_events mirror of milk, RLS: buyer-only thread INSERT / participants read+insert messages / reporter-only report read, RPCs: `get_or_create_gear_thread` SECURITY INVOKER w/ own-listing guard + `list_my_gear_threads` STABLE w/ DISTINCT ON last message + unread CTE + listing-cover join + `mark_gear_thread_read` SECURITY DEFINER + `ack_gear_safe_meeting` buyer-only SECURITY INVOKER). gear API extended (`getOrCreateGearThread` / `listMyGearThreads` / `getGearThreadMessages` / `sendGearMessage` / `markGearThreadRead` / `ackGearSafeMeeting` / `submitGearReport` / `hasAcceptedGearLegal` / `recordGearLegalAcceptance` / `logGearEvent` + `GearReportReason` 8-member union + `gearReportReasonLabel` + `GEAR_LEGAL_DOC_VERSION`). Components: SafeMeetingGuideModal (scroll-gated; 6 sections — public meet / bring someone / inspect / cash-P2P / trust gut / if wrong), GearLegalDisclosureModal (3-step informed consent mirroring milk — Addendum scroll-gate + 3 individual chip acks [not_a_seller, no_recalls, own_risk] + success; fires `gear_legal_addendum_{shown,accepted}` events), ReportListingModal (8 reason chips + 10-2000 char body + silent-to-seller alert copy + 24hr SLA promise). Screens: GearMessageThreadsScreen (FlashList inbox w/ cover image + buyer/seller tag + unread count + useFocusEffect refresh + empty-state "Browse gear" CTA), GearMessageDetailScreen (30s poll + optimistic send + mark-read on mount + listing banner w/ "View listing" deeplink + side label + off-platform warning empty hint). GearListingDetailScreen wired: two-gate flow (per-user Addendum acceptance check → open legal modal if needed → `getOrCreateGearThread` → if `safe_meeting_ack_at=null` open SafeMeeting modal → `ackGearSafeMeeting` + record per-thread safe_meeting acceptance → navigate to chat); own-listing message button disabled; ⚑ Report CTA added below stats (hidden on own listings). GearNavigator + browse header Inbox link wired. Typecheck + lint clean. |
| G7 | ✅ Done | migration 025 (`daily_checkins` w/ mood_score 1-5 + energy_score + user_response ≤1000 + ai_reply ≤800 + crisis_flagged + crisis_resources JSONB + UNIQUE(user_id, checkin_date), `home_feed_cache` w/ cards JSONB + expires_at + generator_version, RLS own-only on checkins + service-role on cache, RPCs: `upsert_daily_checkin` SECURITY INVOKER + `get_today_checkin` STABLE + `get_home_feed` w/ is_stale flag + `list_active_home_users` service-role-only, pg_cron jobs: `ai-milestone-explainer-weekly` (Sun 05:10 UTC) + `home-feed-curator-daily` (09:10 UTC) + `daily-checkin-reminder` (12:00 UTC → user_notifications_feed inserts, reuses V1 push-notify infra, no new OneSignal code needed). 6 new Edge Functions: `ai-milestone-explainer` (Sonnet batch — fills `milestone_library.ai_summary_cache` for NULL or >30d, 2-paragraph second-person, enforced "not medical advice" disclaimer via /medical advice/i regex), `ai-daily-checkin` (Haiku real-time — loads checkin + users.pregnancy_stage + baby_profiles_with_week, 2-4 sentence warm reply, hardcoded crisis routing: suicidal/self-harm→mental_health+crisis_text+postpartum, PPD→postpartum+mental_health, medical emergency→emergency, abuse→emergency+crisis_text, PATCHes row with ai_reply/crisis_flagged/crisis_resources, fallback on any error), `ai-perk-recommender` (Haiku — up to 25 active brand_deals ranked by stage+week+feeding_method, returns {ranked:[{deal_id,reason}]} ID-allowlist filtered), `ai-event-relevance` (Haiku — candidate_event_ids ranked top N w/ ≤12-word reasons), `ai-gear-tip` (Haiku — hardcoded ALLOWED_CATEGORIES matching migration 012 allowlist, prompt forbids car_seat/breast_pump/sleep_positioner/helmet, returns {tip, category_hint} validated), `home-feed-curator` orchestrator (modes: 'single'/'batch', 6 card builders in Promise.all w/ .catch→null fail-soft: milestone+checkin+events+perks+gear_tip+quickaccess, 24h TTL). home API extended (`getTodayCheckin`, `submitCheckin` → upsert_daily_checkin → invoke ai-daily-checkin, `getHomeFeed`, `refreshHomeFeed`) + types DailyCheckin, CrisisResources, HomeFeedCard, 5 block payload types + MOOD_OPTIONS constant. `useHomeStore` rewritten — Promise.all-loads profile+feed+checkin, triggers background refreshFeed when stale. Screens: DailyCheckinScreen (modal — 5 mood chips + energy dots + 1000-char TextInput + disclaimer box + submit → navigation.replace('CheckinResponse')), CheckinResponseScreen (renders crisis card w/ tel:/sms: deeplinks via Linking.openURL when crisis_flagged + Villie reply card), DiscoverHomeScreen (6-tile router: milk/experts/gear/events/perks/help — tab jumps via getParent(), AIHelpChat via getParent()?.getParent()), HomeScreen **fully rewritten ~460 lines** (CheckinBanner 3 states pending/answered/crisis w/ crisis-colored border, MilestoneHeroFromFeed prefers curator long_copy over G1 fallback, BabySnapshotCard unchanged, EventsFeedCard renders 2 ranked events w/ reasons, PerksFeedCard renders 3 ranked perks, GearTipCard w/ optional "Browse <category> →" deeplink, DiscoverCard, G1 teaser fallback when cards.length===0). HomeNavigator extended with 3 G7 routes (DailyCheckin modal, CheckinResponse, DiscoverHome). Typecheck clean. |
| G8 | ✅ Done (cash-only path) | **Decision 2026-04-23: cash-only MVP** (Risk & Compliance §2.7 NN#5 — avoids FinCEN money-transmitter classification). NO Stripe Connect, NO gear-purchase-intent, NO GearCheckoutScreen, NO 48h hold — all Stripe/buyer-protection paths explicitly deferred to post-MVP. Cash-only UX copy was already comprehensive from G6: SafeMeetingGuideModal "Cash or P2P only" section (Venmo/Zelle/Apple Cash at pickup), GearLegalDisclosureModal "No payment processing" section w/ chargeback/dispute disclaimer, GearMessageDetailScreen off-platform warning. **Analytics instrumentation**: added `gear_listing_viewed` (detail load w/ category + cpsc_recall_status), `gear_listing_saved`/`gear_listing_unsaved` (toggleSave), `gear_listing_created` (post-insert w/ category + has_upc + is_free + image_count + year_manufactured), `gear_cpsc_check_result` (status + has_upc + recall_number on every CPSC call), `gear_cpsc_block_shown` (recall hard-block renders — **legal audit trail for CPSIA §19 enforcement**), `gear_barcode_scanned` (upc + found + source), `gear_vision_identified` (confidence + has_name + category_hint), `gear_thread_opened` (thread_id + listing_id + buyer/seller side on mount), `gear_message_sent` (body_len + side on successful send). Existing G6 events still fire: gear_safe_meeting_{shown,accepted}, gear_legal_addendum_{shown,accepted}, gear_listing_reported. All events write to `gear_analytics_events` via `logGearEvent` — user_id + event_name + properties JSONB. App-store update is a deployment action, not code. Typecheck + lint clean. |

### V4 Open gates (must resolve before the phase they block)
- **G3 affiliate network activation** (post-MVP, before real conversion revenue): `brand_deals` carries an `affiliate_network` enum and `{subid}` URL-template plumbing. Per-network webhook verification is now **code-complete** in `perks-redemption-webhook` — `network` field in body routes to the matching verifier: `impact` (HMAC-SHA256 of raw body via `IMPACT_WEBHOOK_SECRET`, header `x-impact-signature`), `shareasale` (HMAC-SHA256 of `secret:timestamp:method:path` via `SHAREASALE_API_SECRET`, headers `x-shareasale-date` + `x-shareasale-authentication`, ±300s drift guard), `cj` (IP allowlist via `CJ_ALLOWED_IPS` against `x-forwarded-for` left-most), `direct` (legacy shared-token via `PERKS_WEBHOOK_SECRET`, header `x-village-webhook-token`). Raw body is read once via `req.text()` so Impact HMAC verifies exact bytes; JSON is parsed from the cached string. Timing-safe comparisons throughout; reject reasons are console-logged but never returned to the caller. To go live: (a) sign contracts with Impact / ShareASale / CJ per brand; (b) set the matching secret(s) in Supabase Secrets; (c) replace in-SQL deeplink resolution (`claim_perk`) with Edge-Function-based resolution (`perks-build-deeplink`) if/when network-specific SubID macros beyond `{subid}` are needed. No further code change needed until signed — attribution will simply stay at `status='clicked'`. FTC disclosure copy is already live on every claim.
- **G2 event ingestion** (pipeline shipped, awaiting feeds): partner-feed pipeline now landed via migrations 045 (`events_partner_feeds` + ICS dedup constraint + pg_cron 09:00 UTC), 046 (review pipeline + `find_duplicate_event` RPC), 047 (`needs_geocode` flag), 048 (cross-feed dedup hash). Three deployed edge functions: `events-ingest-ics` (puller), `events-geocode` (Google Geocoding fallback), `ai-event-screen` (Sonnet review pass). `events_partner_feeds` is **service-role-only by design** (Supabase advisor flags as `rls_enabled_no_policy` — false positive; mobile has no business reading the registry). To go live: (a) ops adds rows to `events_partner_feeds` via Studio with the source ICS URL + `is_active=true` + region; (b) optional — set `app.supabase_url` + `app.service_role_key` Postgres GUCs so the pg_cron HTTP callout actually fires (or invoke the GH Action cron in `.github/workflows/supabase-crons.yml` per memory `project_hosted_deploy.md`); (c) confirm Google Geocoding API key in Supabase Secrets for `events-geocode`. Falls back cleanly to manual curation while feeds are off.
- ~~Before **G4**: confirm final prohibited-items enum~~ ✅ Resolved in G4 via allowlist CHECK enum + `toy_year_safe` (≥1978 CPSIA) + `crib_year_safe` (≥2011 drop-side rule). Excluded: car_seat, breast_pump, sleep_positioner, inclined_sleeper, helmet.
- ~~**G4 Supabase Storage setup**~~ ✅ Resolved 2026-04-27 — `gear-listings` bucket exists on hosted (public read) with the documented owner-folder INSERT/UPDATE/DELETE RLS pattern (`auth.uid()::text = (storage.foldername(name))[1]`). `avatars` bucket created the same way for A2 avatar upload.
- Before **G5** public rollout: attorney P1 review of Gear Marketplace Terms Addendum; P2 CPSC Prohibited Items Policy; FDUTPA review of all Gear in-app copy; insurance GL+E&O confirms marketplace coverage; 24hr takedown SLA assignee named.
- Before **G8**: **FinCEN / Florida money-transmitter counsel review** — can The Village process P2P gear payments at MVP? Risk doc says NO; Master Plan assumes YES. Biggest V4 architectural fork.
- Cross-cutting: eBay Marketplace Insights API application **should be submitted now** (4–6 wk approval blocks G5 smart pricing; depreciation-table fallback ships in interim).
- **Migrations 051–054 — security advisor sweep** (applied to hosted on 2026-05-17):
  - `051_security_function_search_path.sql` — pins `search_path = public, pg_catalog` on 42 app-owned public functions flagged by `function_search_path_mutable`. Behavior-preserving.
  - `052_security_revoke_anon_rpc_execute.sql` — addresses `anon_security_definer_function_executable` × 33 app-owned + `authenticated_security_definer_function_executable` × 36 by revoking EXECUTE from PUBLIC and re-granting per-role: 28 mobile RPCs → authenticated + service_role; 3 cron/ops RPCs (`list_active_home_users`, `rls_auto_enable`, `verify_app_gucs`) → service_role only; 2 trigger-internal (`handle_new_auth_user`, `scan_room_message_async`) → no grant (triggers don't check EXECUTE). Skips PostGIS extension-owned `st_estimatedextent` overloads (cannot ALTER extension functions). Note: `function_body_check_rights` admin/moderator RPCs (`is_clinical_reviewer` etc.) keep their internal role guards — the migration only narrows network-level reachability.
  - `053_security_storage_scope_select.sql` — drops the `*_public_read` SELECT policies on `avatars` + `gear-listings` and replaces with `authenticated`-scoped SELECT. Public-URL reads via `<Image>` are unaffected (bucket `public=true` bypasses RLS through the CDN endpoint); `.list()` / `.download()` from anon callers is now blocked. Verified no client code calls `.list()` on either bucket.
  - `054_security_revoke_explicit_role_execute.sql` — **fixes the no-op in 052**. Discovered after deploy: Supabase issues *explicit* default grants to `anon`/`authenticated`/`service_role` on every function (verified via `pg_proc.proacl`), so `REVOKE FROM PUBLIC` in 052 had nothing to revoke and all 33 anon-RPC lints persisted. 054 explicitly `REVOKE FROM anon` for mobile-callable RPCs and `REVOKE FROM anon, authenticated` for service-role-only + trigger-internal functions. Apply alongside 051–053 for full effect.
  - Migration 062 (2026-05-18) re-revoked anon on `get_weekly_journey` after audit showed 054 didn't stick for that function — cause unknown.
- **Accepted advisor warnings (not fixable in code):**
  - `public_bucket_allows_listing` × 2 — fires on `storage.buckets.public=true` itself, not on RLS. The only way to silence it is to make `avatars` + `gear-listings` private and switch every `getPublicUrl()` call to `createSignedUrl()` (large refactor with TTL management for cached image components). Public buckets are appropriate for these surfaces (avatars + gear photos already broadcast publicly via the app); accepting the warn is the right trade-off for now.
  - `events_partner_feeds` `rls_enabled_no_policy` — documented false positive (service-role-only registry; see migration 045 header).
  - `specialist_invites` `rls_enabled_no_policy` — same shape as `events_partner_feeds`. Service-role-only invite registry written by the Village team, read by anon via the `get_specialist_invite_by_token` SECURITY DEFINER RPC. RLS is enabled with no policy on purpose so direct PostgREST reads always fail (see migration 060 header).
  - `get_specialist_invite_by_token` `anon_security_definer_function_executable` — **intentionally anon-callable**. The specialist-invite flow hands a token-URL to an unauthenticated email recipient; they must be able to look up their invite before signup. The RPC only returns rows when `used_at IS NULL AND revoked_at IS NULL AND expires_at > now()` and only on exact-token match — there is no enumeration vector. Accept.
  - `extension_in_public` × 4 — `postgis`, `pg_net` etc. installed in `public` schema. Moving them is risky (breaks every PostGIS reference in app code) and Supabase tooling expects them there. Accept.
  - `spatial_ref_sys` `rls_disabled_in_public` — PostGIS-owned reference table; we don't own the DDL and enabling RLS on an extension table breaks every PostGIS query. Accept (rolls up under `extension_in_public:postgis`). **Confirmed 2026-06-10**: migration 089 tried `ALTER TABLE ... ENABLE RLS` and it failed with `must be owner of table spatial_ref_sys` — genuinely unfixable from our role. The recurring "Table publicly accessible" advisor EMAIL can only be silenced via Supabase dashboard → Account/Project → Notifications (turn off Security advisor digest). No user data in this table.
  - **Migrations 089 + 090 (2026-06-10 advisor sweep over the 072–088 surface):** pinned `search_path` on `auto_withdraw_p0_overdue_listings` + `manual_pieces_set_updated_at`; revoked anon + PUBLIC EXECUTE on `get_gear_listing`, `list_manual_pieces`, `list_my_gear_listings` (now authenticated+service_role only). Kept anon-by-design: `get_manual_video_share_meta`, `get_specialist_invite_by_token`.
  - `st_estimatedextent` × 6 (3 anon × 3 authenticated overloads) `*_security_definer_function_executable` — PostGIS extension-owned C functions. Cannot ALTER extension functions. Accept (documented in migration 052 header).
  - `authenticated_security_definer_function_executable` × ~27 app RPCs (`approve_*`, `claim_perk`, `get_my_*`, `join_room`, `list_my_*`, `mark_*_read`, `resolve_crisis_flag`, etc.) — these are the legitimate mobile-callable RPCs we kept on `authenticated` in migration 054 by design. Each has an internal `auth.uid()` / `is_moderator_anywhere()` / `is_clinical_reviewer()` guard. The lint is informational — it just flags any SECURITY DEFINER reachable by signed-in users, regardless of internal checks. Accept.
  - `auth_leaked_password_protection` — HaveIBeenPwned-backed leaked-password screening is a **Supabase Pro-tier feature**. We are on Free; the toggle is not available in the dashboard, so this is not an action item, it is a plan constraint. Accept until plan upgrade. If/when we move to Pro: Auth → Providers → Email → Password security → "Prevent use of leaked passwords".

## Me / Account — Build Progress
| Phase | Status | Notes |
|---|---|---|
| A1 | ✅ Done | `MeNavigator` (native stack: MeRoot + EditProfile). `MeScreen` expanded from crisis-only → profile header (avatar/name/email/stage pill) + **Edit** CTA + Baby card (name + age + current week) + **My stuff** cross-tab deep-links (Saved specialists → Experts/Favorites; My events → Home/MyRsvps; My perks → Home/MyClaims; Milk inbox → Milk/MilkMessageThreads; Milk orders → Milk/MilkOrders; Saved donors → Milk/SavedDonors; My gear listings → Gear/MyListings; Saved gear → Gear/SavedGear; Gear inbox → Gear/GearMessageThreads — uses `navigation.getParent().navigate(tab,{screen})`) + Preferences (EN/ES language toggle w/ optimistic revert) + Crisis resources + Sign out. `EditProfileScreen` edits mutable user fields (full_name, pregnancy_stage, due_date conditional on stage, zip_code, insurance_provider) straight to `users` via supabase + optimistic `setProfile` with revert on error; discard-changes confirm on Cancel when dirty. Tab wiring switched `Tab.Screen "Me"` from `MeScreen` → `MeNavigator` in `AppNavigator.tsx`. `fetchProfile` runs on MeRoot mount so cold launch is populated. Typecheck clean. |
| A2 | ⚪ Pending — **scoping phase** | **Heavier settings gaps vs `docs/source/Village_Onboarding_UX.md` Profile/Settings spec**. Three subphases — none are code-complete. Decide scope + gate order before starting: |
| A2.a — Distance / radius preference | ✅ Done | migration 031 adds `users.search_radius_miles INT DEFAULT 25 NOT NULL CHECK (BETWEEN 1 AND 100)`. `constants.ts` exports `DEFAULT_SEARCH_RADIUS_MILES` / `MIN` / `MAX` / `MILES_TO_KM` / `RADIUS_CHOICES_MILES` ([5,10,25,50,75,100]). `store/user.ts` adds `search_radius_miles` to `UserProfile` (+ select list) and exports `getPreferredRadiusMiles()` / `getPreferredRadiusKm()` synchronous store reads with cold-start fallback. API call sites use the pref as fallback when caller doesn't pass an explicit radius: `specialists.ts` (was 10), `milk.ts:searchDonorsNear` (was 25), `events.ts:listUpcoming` (was 50km, converts mi→km), `gear.ts:listGear` (was 50km, converts mi→km). New `RadiusPreferenceScreen` under `MeNavigator` (6 chip choices, optimistic write to users + revert on error). MeScreen "Preferences" section gains a "Search radius" row showing the current value + description of what it affects. Inline filter UIs in milk (FilterDrawerModal RADII, MilkMatchScreen) still present for per-search override — they no longer fall back to hardcoded 25 via this pref plumbing (they set their own state). Typecheck clean. Migration applied. Telemetry event wiring deferred. |
| A2.b — Notification toggles + quiet hours | ✅ Done | migration 032 adds `users.notif_prefs JSONB NOT NULL DEFAULT '{"events":true,"groups":true,"specialists":true,"milk_hub":true,"articles":true,"ai":true,"promotions":false}'::jsonb` — keys mirror UX spec §Settings; `promotions` defaults FALSE per FTC/CAN-SPAM opt-in rule. `store/user.ts` exports `NotifPrefKey`, `NotifPrefs`, `DEFAULT_NOTIF_PREFS`, adds `notif_prefs` to `UserProfile` + SELECT list + spread-merge with defaults on fetch (so a newer key against older row reads as default not undefined), exports `getNotifPrefs()` sync helper. `NotificationPreferencesScreen` (7 `Switch` rows w/ per-row title/description + opt-in note on `promotions`; optimistic write to users + revert on error; per-row ActivityIndicator saving state). Wired into `MeNavigator` + `MeScreen` Preferences section as a new row below Search radius. **Call-site gates (3 senders):** `appointment-reminder` — checks `users.notif_prefs.specialists` and marks `twilio_reminder_sent=true` on opt-out to prevent every-15m retry; `room-weekly-summary` — after filtering `room_members.notif_pref='all'`, looks up `users.notif_prefs.groups` for each candidate and filters before `push-notify` fan-out; `milk-safety-screener` — gates the auto-deactivate SMS on `users.notif_prefs.milk_hub` (the DB-side pause still fires; only the outbound SMS is silenced). **Explicitly NOT gated (transactional/compliance):** `admin-approve-specialist` (account status), `room-message-scan` moderator crisis SMS (safety-tier), milk/gear 1:1 thread sends (direct-message surfaces). **Due-date input unification (Onboarding + EditProfile):** Both due-date entry surfaces now share `apps/mobile/src/utils/dueDate.ts` — `formatDueDateInput` masks user typing into MM/DD/YYYY shape, `isPlausibleDueDate` checks shape + month 1–12 + day 1–31 + year within currentYear ± {-1, +2}, and `toIsoDate`/`fromIsoDate` convert at the DB boundary (Postgres `DATE` is canonical YYYY-MM-DD). OnboardingProfileScreen previously sent the masked string straight into the column (relying on PG's `DateStyle MDY` to parse it) — now explicitly converts to ISO before insert; if the user used Skip with malformed input, `toIsoDate` returns null and the column is cleared rather than coerced. EditProfileScreen previously used `YYYY-MM-DD` placeholder + free-text; now matches Onboarding's MM/DD/YYYY mask + error styling, blocks save with an Alert when malformed, and converts both directions at the boundary so existing ISO rows display correctly. Continue/Save still pass when empty (field is optional). Pure JS — no new native dep + no EAS rebuild required. **Onboarding funnel analytics:** OnboardingProfileScreen now fires four events via `useAnalytics`: `onboarding_step_view` on every step impression (drop-off between view counts reveals where users abandon), `onboarding_step_advanced` on forward navigation (carries `step` + selected `stage`), `onboarding_complete` on success (carries `stage` + `has_due_date`/`has_zip`/`has_insurance` flags so we can measure optional-field skip rate and decide whether to drop those steps), and `onboarding_failed` with truncated reason on `authService.updateProfile` errors. New event types added to `AnalyticsEvent` union and `EventProperties` extended with funnel fields (`step`, `total_steps`, `stage`, `has_*`, `reason`). Sentry breadcrumbs flow as before; no server-persist gate (these are non-compliance events). **Profile completion meter (Me):** MeScreen renders a small rust-bordered card between the header and "Your baby" showing `Profile · {n}%` + a thin progress bar + "Add {missing fields} for better matches" hint, tappable to open EditProfile. Hidden at 100% and on cold launch (profile === null). Counts: name, stage, ZIP, insurance, plus due_date (only when stage is 1st/2nd/3rd trimester — TTC and postpartum stages don't need it, so a TTC user can still hit 100%). **Sentry user tagging:** `useAuthStore.setSession` now calls `Sentry.setUser({ id })` (and clears on signOut) so production error events are attributable to a Supabase user via id only. Email + username remain stripped server-side via the existing `beforeSend` hook — id-only is HIPAA-cautious and the minimum context to triage. Wrapped in try/catch so Sentry errors can never break the auth flow. **ExpertsHome polish (V1):** ExpertsHomeScreen now uses the user's saved search radius (A2.a `getPreferredRadiusMiles()`) instead of the hardcoded 15-mile fallback, and conditionally inserts a "🩺 {insurance_provider}" filter chip immediately after "All" when `users.insurance_provider` is set. The chip applies the existing `insurance_filter` parameter on `specialists_near` (already case-insensitive LIKE against `specialist_insurances.insurance_name`). No new schema — reuses RPC + profile field that were already in place. **Quiet-hours Home indicator** — `apps/mobile/src/utils/quietHours.ts` mirrors the shared edge-function helper client-side, and HomeScreen renders a small olive "🌙 Quiet hours · until 7 AM" pill below the greeting whenever `notif_prefs.quiet_hours.enabled` is true and the user is currently inside the window. Reads from `useUserStore`, re-evaluates on profile change (no wall-clock interval — passive indicator). **Admin compliance-event viewer** — service-role-gated `admin-compliance-events` edge function returns paginated `gear_analytics_events` + `milk_analytics_events` (filterable by source/event_name/user_id/since/until + JSON or RFC-4180 CSV export). Every call writes to `admin_audit_log` with the filters + return count for chain-of-custody (CPSIA §19 defense + FDUTPA milk review). **Central push-notify pref gate (defense-in-depth)** — `push-notify` accepts an optional `pref_key` (one of the 7 NotifPrefKey values) and `respect_quiet_hours` (default true) on every payload. When set, the function looks up `users.notif_prefs` for each candidate external_id BEFORE the OneSignal fan-out and filters out anyone with the key set to `false` or currently in their quiet-hours window. Crisis/transactional callers (admin-approve-specialist, room-message-scan moderator alerts) pass `bypass_prefs:true` to skip both gates. Per-caller gates remain primary (so they can early-exit before AI/SMS spend); the central gate is the safety net so future callers can't silently violate prefs by forgetting to filter. **OneSignal tag sync** also lands the user-set switches onto the device record so the marketing/lifecycle team can segment from the OneSignal dashboard without writing new edge functions. `useOneSignal` re-runs `OneSignal.User.addTags(...)` whenever `useUserStore.profile` changes — tags include `pregnancy_stage`, `preferred_language`, `pref_*` for all 7 notif keys (boolean→string), and `quiet_hours_enabled`. Server-side filtering via OneSignal `filters: [{field:'tag',...}]` is now possible as an alternative to the per-user DB lookup; existing call-site DB gates remain authoritative for safety-critical correctness. **Polish batch 2026-04-24 (root error boundary + CreateListing a11y):** **App-wide error boundary** — `apps/mobile/src/components/shared/ErrorBoundary.tsx` (class component using `getDerivedStateFromError` + `componentDidCatch`) wraps `<RootNavigator/>` in `App.tsx`. On render error: shows brand-aligned cream fallback w/ "Try again" button that resets boundary state, captures the exception to Sentry with `tags.boundary='root'` + `contexts.react.componentStack`, and surfaces the error to console + dev redbox in `__DEV__`. Both Sentry capture and the fallback are wrapped in try/catch so telemetry can never break the recovery UI. Fallback is dependency-free (no navigation hooks, no store reads) so it works even when the underlying state is the thing that crashed. **CreateListingScreen a11y completion** — pass over the seller's primary form: Cancel button (role + descriptive label), Scan barcode button (busy/disabled state + dynamic label "Looking up barcode" / "Scan barcode to autofill"), Identify from photo button (state-aware label including "add a photo first" hint when disabled + busy state), subcategory chips (`accessibilityState.selected` + "Type: X" labels — already had role), photo thumbnails (`accessibilityLabel` "Listing photo N of M") + remove buttons ("Remove photo N"), photo add button ("Add photo, N of 5 added"), free-toggle Switch (role/label/state.checked), location button (state.selected + dynamic label "Location set, tap to update" / "Use my current location"), Post listing submit (busy/disabled state + dynamic label). Other chips (categories, conditions, age tags) already had role + selected from G4. Typecheck clean.

**Polish batch 2026-04-24 (notif-pref telemetry + upload progress):** **Notification-pref telemetry** — `useAnalytics` extends `AnalyticsEvent` with `notification_pref_changed` + `quiet_hours_changed`, and `EventProperties` with `pref_key`/`enabled`/`start_hour`/`end_hour`. NotificationPreferencesScreen fires `notification_pref_changed` after each successful Switch save (carries which key + new value), and `quiet_hours_changed` after each quiet-hours mutation (carries enabled + start/end hours). Both events are fire-and-forget Sentry breadcrumbs — no server persist (these are UX signals, not compliance-critical). Errors short-circuit before the event fires so we never log a "changed" event for a write that was reverted. **CreateListing upload progress UI** — multi-photo posts on slow networks used to show only an unbranded `<ActivityIndicator>` for 5-10s, leaving the seller wondering if the app froze. Now: `uploadProgress` state ({current, total}) gates a "Uploading photo N of M" label next to the indicator inside the Post button, plus a thin rust-filled progress bar (`role=progressbar`) under the footer that grows as each photo finishes uploading. Sequential upload loop bumps the counter before each `uploadImage` call. State resets to null on success and on error (finally block). Pure JS — no native module needed.

**Polish batch 2026-04-24 (avatar upload + Me · Account & security):** **EditProfile avatar upload** — migration 034_a2_avatars_storage.sql creates the public-read `avatars` Storage bucket (2 MB cap, image MIMEs only) with the same owner-folder RLS pattern as gear-listings (`auth.uid()::text = (storage.foldername(name))[1]`). EditProfileScreen renders a tappable avatar block at the top of the form: `<Image source={{ uri: avatar_url }} />` w/ pencil edit-badge or initial fallback. Tap fires `expo-image-picker.launchImageLibraryAsync({ allowsEditing: true, aspect: [1,1], quality: 0.8 })`, fetches the local URI → `arrayBuffer()` → `supabase.storage.from('avatars').upload({user_id}/{Date.now()}.{ext}, buf, { contentType, upsert:false })`, then writes the public URL back into `users.avatar_url`. Optimistic store update via `useUserStore.setProfile` so MeScreen's avatar refreshes immediately on goBack. Date-keyed filenames sidestep Storage caching with `upsert:false`. Helper text on the screen now points users to "Account & security in the Me tab" for email/password changes (the "coming soon" placeholder is gone). **Me · Account & security** — new section between Preferences and Crisis resources, two rows: "Change email" (shows current email under the label) and "Change password". `ChangePasswordScreen` uses `supabase.auth.updateUser({ password })` with min-8-char + confirm-match validation, success Alert + back-navigate, errors stay open for retry; we don't re-prompt for the current password because the active session is the auth proof (matches Supabase's documented posture — wire a step-up reauth here if SOC2 ever requires it). `ChangeEmailScreen` uses `supabase.auth.updateUser({ email })` and tells the user to confirm via the link sent to the new inbox; the existing trigger syncs `auth.users.email → public.users.email` after confirmation, so we don't optimistically mirror the new email. Same-as-current and invalid-format are blocked client-side. Both screens are wired into MeNavigator (`ChangePassword` + `ChangeEmail` routes). Typecheck clean.

**Polish batch 2026-04-28 (discharge handoff — onboarding tone + ES audit + welcome card):** Three-task sweep before broader UI/UX work. **(1) ES audit on discharge surfaces** — 9 fixes to `i18n/es.json`: grammatical (`Tu cuerpo se está sanando` → `Tu cuerpo está sanando` — `sanar` is intransitive), idiomatic (`Apoyo real` → `Apoyo de verdad` — calque), agreement (`Buz\u00f3n de leche` → `Buz\u00f3n Milk Hub` — uses unicode-escaped form because `me` section is mixed-encoding while `home`/`checkin`/`crisis` use raw UTF-8), mood-scale ordinal collapse (`Más o menos / Bien / Buena / Genial` → `Regular / Bien / Muy bien / Excelente` — fixed bare feminine `Buena` and the Bien/Buena pseudo-collapse), and a "capacitados" agreement bug on the crisis sheet lead (resources can't be trained — rewrote attribution to the people behind the lines). Also synced PSI/988 punchier ES copy with EN. **(2) Onboarding tone pass** — 5 edits across EN+ES: `signUp.title`/`sub` ("Create your account / Join moms across your community" → "Let's set you up / A few details and you're in.") and `onboarding.stageTitleLine1/Line2/stageSub` ("Where are you in your journey?" → "How old is your baby?") — onboarding is postpartum-only as of 2026-04-27, so the generic `journey` question becomes the more direct baby-age question that fits the actual chip choices (`postpartum_0_6mo` / `postpartum_6_12mo` / `postpartum_1yr_plus`). Skipped `onboardingSlides.welcomeTitleLine1/Accent` ("Tu aldea te espera") — flagged as brand-voice judgment (poetic ES vs. the brand's untranslated "The Village" elsewhere). **(3) Discharge welcome card** — one-shot orientation card on Home for the hospital-handoff moment. Renders ABOVE the daily-checkin banner (so a brand-new mom sees a soft "you're in the right place" beat before being asked how she's feeling), gated on `earlyPostpartum && welcomeDismissed === false` (same gate as the always-here crisis card — postpartum_0_6mo + week ≤ 6 OR no baby profile yet). Soft cream-on-cream card with rust-tinted eyebrow, `×` dismiss top-right + "Got it" pill CTA both fire the same dismiss handler. State persisted via AsyncStorage key `village.dischargeWelcomeDismissed.v1` (key is versioned so a future re-themed orientation card can re-show to already-dismissed users). Storage write is fire-and-forget (failure → card returns next launch — acceptable degradation since alternative is showing forever); read fail-safe → hide. New analytics event `home_discharge_welcome_dismissed` (Sentry breadcrumb only). Copy is clinician-handoff-grade EN+ES per the hospital memory — names the discharge moment, sets expectations for first six weeks, points at the in-app surfaces that carry the user through it. Pure JS — no schema, no native deps. Typecheck clean. JSON validates clean.

**Polish batch 2026-04-27 (Home — early-postpartum crisis card):** Hospital-discharge GTM puts users in their highest-risk PPD/PPA window (postpartum 0–6 weeks) at first launch. Adds a calm always-here crisis card to HomeScreen, gated on `pregnancy_stage === 'postpartum_0_6mo'` AND `baby_profiles.current_week_number ≤ 6` (or no baby profile yet — fresh signup case). Card sits between CheckinBanner and the milestone hero so it's visible without scrolling. Olive accent (matches the "always here" palette) rather than rust — preventive, not reactive, so a non-crisis user doesn't read it as a warning. Tap opens the existing `CrisisResourcesSheet` (reused from V3 C4 chat crisis routing — 988, Crisis Text Line, PSI, 911) with a softer postpartum-specific lead ("You don't have to be in crisis to use these. They're free, confidential, and trained for new moms.") so the sheet doesn't feel alarmist when reached from Home rather than from a flagged check-in. New analytics event `home_crisis_card_opened` (Sentry breadcrumb only — not compliance-critical). EN + ES copy is clinician-handoff grade per the hospital memory. The same resources remain reachable from Me and from the daily-checkin crisis verdict path; this card is purely a discoverability lift for the highest-risk window. Pure JS — no native or schema changes. Typecheck clean.

**Polish batch 2026-04-24 (auth polish — Login + SignUp):** Discharge-handoff context: a clinician hands the phone to a postpartum mom who's tired and one-handed (likely with a baby in the other arm). The signup/login flow is the very first interaction after Splash + Onboarding. Three small, high-leverage gaps: (1) **Show/Hide password toggle** — both screens add a "Show"/"Hide" tap target overlaid on the password TextInput right edge, controlled by `showPassword` state, flipping `secureTextEntry`. Reduces typo-failures for sleep-deprived users without compromising security (default remains masked; toggle requires explicit tap). a11y label flips between "Show password" and "Hide password" with `accessibilityState.selected`. (2) **Email format validation on SignUp** — `EMAIL_RE` regex (loose RFC-5321: `non-whitespace@non-whitespace.non-whitespace`) gates submission with a clearer Alert ("Check your email — that doesn't look like a valid email address.") + inline rust border + "Enter a valid email address" hint as the user types. Saves the round-trip-to-Supabase-and-back that previously surfaced as a generic auth error. (3) **a11y on Sign In / Create Account buttons** — `accessibilityRole="button"` + dynamic `accessibilityLabel` ("Signing in" / "Sign in", "Creating account" / "Create account") + `accessibilityState.busy` while loading so VoiceOver announces the in-flight state. Forgot Password link gets `accessibilityRole="link"` + descriptive label. Pure JS — no native or schema changes. Typecheck clean.

**Polish batch 2026-04-24 (empty-state pass — Gear, Perks, Events, Milk):** Pre-launch hospital-discharge GTM (project memory `project_hospital_discharge_distribution.md`) means a new postpartum user lands cold and clicks through every "My X" tab in the first session. Every blank tab read as broken: just a small grey title + button (or nothing). Pass over 5 list screens to land the FavoritesScreen pattern — emoji + bold title + body explaining what shows up here + rust pill CTA back to the relevant browse surface. Touched: **MyListingsScreen** (📦 "Nothing listed yet" + outgrown-gear copy + "＋ List an item" → CreateListing); **SavedGearScreen** (♡ "Nothing saved yet" + "Tap the heart on any listing" + "Browse gear" → GearBrowse); **MyClaimsScreen** (🎁 "No perks claimed yet" + stage-match copy + "Browse perks →" → PerksList); **MyRsvpsScreen** (📅 "No upcoming RSVPs" + meetup-and-webinar copy + "Discover events →" → EventsList; past tab gets 🗓️ "No past events" with a softer no-CTA message since there's nothing to drive to); **MilkOrdersScreen** (📦 "No orders yet" + verified-donor copy + "Find a donor →" → DonorSearchList — order screen previously had the emoji+title+body but no CTA, leaving a recipient stuck). Copy tone is clinician-handoff-grade per the hospital memory — no flippant marketing voice. Style block is consistent across all five (52pt emoji, 20pt 800-weight title, 14pt 21-line-height body, rust 14-radius pill CTA at 24/14 padding) so first-impression density feels uniform across tabs. Pure JS — no schema or native dep changes. Typecheck clean.

**Polish batch 2026-04-24 (Sentry stage + ZIP validation + a11y + skeletons):** **Sentry stage/language context** — `useUserStore.setProfile` now calls `Sentry.setContext('user_meta', { pregnancy_stage, preferred_language })` (cleared to null on signOut path) so production triage can group errors by stage without putting any non-id PII on the Sentry user object. Wrapped in try/catch — telemetry must never break the app. **ZIP validation utility** (`apps/mobile/src/utils/zip.ts`) — `formatZipInput` masks 5-digit + ZIP+4 (`12345-6789`), `isPlausibleZip` regex-checks. OnboardingProfileScreen + EditProfileScreen both use the mask, render an `inputError` border + "5-digit ZIP (or ZIP+4)" hint when malformed, and gate the continue/save button on `zipOk`. **Gear/Milk a11y pass** — back buttons (`accessibilityLabel="Back"` + role) on MilkOrders, MilkPurchase, GearMessageDetail; qty stepper on MilkPurchase (decrease/increase by four labels + `accessibilityLiveRegion="polite"` on the oz display); fulfillment radios on MilkPurchase (`accessibilityRole="radio"` + `accessibilityState={{ selected }}` + descriptive label including SMS-reveal/+shipping-cost); listing/saved/messaged cards in Gear screens (`accessibilityLabel` describing title + state — sold/no-longer-available/status pill); MyListings action buttons (mark sold / withdraw / reactivate role+label). **Skeleton loaders for G2/G3/G4** — extended `components/shared/SkeletonLoader.tsx` with `GearCardSkeleton`, `EventCardSkeleton`, `PerkCardSkeleton` (and `*ListSkeleton` 3-stack helpers). Wired into GearBrowseScreen `ListEmptyComponent` (3 cards), EventsListScreen empty state (2 cards), PerksListScreen empty state (3 cards) — all replace `<ActivityIndicator />` so first-load feels structural rather than blank. ActivityIndicator import dropped from each. Pure JS — no native dep changes. Typecheck clean.

**Quiet hours follow-up (migration 033):** extends `notif_prefs` default JSONB + backfills existing rows with `quiet_hours:{ enabled:false, start_hour:22, end_hour:7, tz:"America/New_York" }`. Shared helper `supabase/functions/_shared/quiet-hours.ts::isQuietHoursActive` handles same-day + night-wrap windows via `Intl.DateTimeFormat` tz conversion (fail-soft on unknown tz → not-quiet, never silently black-hole). UI: NotificationPreferencesScreen gains a "Quiet hours" card — enable Switch + ± hour steppers (12-hour AM/PM display, wraps 0..23) + tz row; first enable adopts device tz via `Intl.DateTimeFormat().resolvedOptions().timeZone`. Sender quiet-hours gates: `appointment-reminder` checks AFTER `specialists` pref and does NOT mark `twilio_reminder_sent` (so the next 15-min cron tick retries inside the window); `room-weekly-summary` now delegates the per-user `groups` pref + quiet-hours check to `push-notify` itself by passing `pref_key:'groups'` + `respect_quiet_hours:true` (system digest card still posts to the feed regardless — only the proactive push is withheld). `milk-safety-screener` is **not** gated on quiet hours (account-status, not nudge). Typecheck clean. Migrations 032 + 033 applied. |
| A2.c — Delete account | 🟡 UI scaffold done · cascade attorney-gated | **Soft-delete UI shipped behind feature flag** (2026-04-27). Edge Function `account-delete` (`supabase/functions/account-delete/index.ts`) — JWT-resolves caller via user-scoped client, then service-role UPDATE sets `users.deleted_at = now()` + `deletion_requested_at = now()` with `.is('deleted_at', null)` for idempotency (a second call from a soft-deleted user returns `already_deleted:true`). Existing RLS `users_select_own` filters `deleted_at IS NULL` (migration 016) so the row becomes invisible immediately and the next session refresh signs the user out. Best-effort `admin_audit_log` row written w/ `action:'account_soft_delete'` + `metadata:{cascade_pending:'attorney-review'}`. Mobile: `accountApi.deleteAccount()` (`apps/mobile/src/api/account.ts`) + `DeleteAccountScreen` (`apps/mobile/src/screens/me/DeleteAccountScreen.tsx`) — typed-DELETE confirm like GitHub/Stripe, two-step: type "DELETE" → tap rust button → Alert.alert confirmation → call edge fn → unconditional signOut (signOut errors logged but non-fatal since soft-delete is already committed). Three analytics events added to `useAnalytics` (`account_delete_requested`/`succeeded`/`failed`). EN + ES i18n keys added under `account.del*` and `me.deleteAccount*`. Wired in MeNavigator + MeScreen Account & security section, **gated on `EXPO_PUBLIC_DELETE_ACCOUNT_ENABLED === '1'`** — discovery row hidden by default; the screen renders fine when navigated directly so QA builds can drive it. Typecheck clean. **Explicitly OUT OF SCOPE pending attorney review** (documented in edge function header): auth.users row deletion via `supabase.auth.admin.deleteUser(id)`, cascade scrub through `favorites`/`reviews`/`appointments`/`milk_*`/`gear_*`/`baby_profiles`/`daily_checkins`/`brand_deals` claims, OneSignal external_id unlink, Stripe Connect account removal. **Pre-flag-flip gates** (in priority order): (1) attorney sign-off on retention policy — what's row-deleted vs PII-scrubbed (see retention rule list under "A-phase open gates"); (2) cascade implementation matching that sign-off; (3) privacy notice update reflecting the cascade behavior. Until then, leave `EXPO_PUBLIC_DELETE_ACCOUNT_ENABLED` unset in production builds. |

### A-phase open gates
- **A2.b default for "promotions"**: must be `false` (opt-in) if any third-party brand promos qualify as marketing under CAN-SPAM/FDUTPA. Keep transactional (appointment reminders, order updates, crisis flags) as always-on — never a toggle.
- **A2.c retention rules**: Before implementing cascade, enumerate which tables are legally retained (milk `milk_transactions`, gear `gear_listings` w/ `cpsc_recall_status`, `milk_analytics_events`/`gear_analytics_events` CPSIA trail, `admin_audit_log`). These get PII-scrubbed on delete, not row-deleted.
- **A2 sequencing**: A2.a is cheapest + unlocks immediate UX wins; A2.b is medium (requires sweep of every push-notify call site — 8+ edge functions); A2.c is highest-risk and should ship last after attorney sign-off. Recommend A2.a → A2.b → A2.c.

| Phase | Status | Notes |
|---|---|---|
| Phase 0 | ✅ Done | Monorepo, Expo, Supabase config, shared types |
| Phase 1 | ✅ Done | Splash, Onboarding (3 slides), SignUp, Login, ForgotPassword, OnboardingProfile |
| Phase 1 | ✅ Done | Splash, Onboarding (3 slides), SignUp, Login, ForgotPassword, OnboardingProfile |
| Phase 2 | ✅ Done | specialists_near RPC, API layer, Zustand store, ExpertsHome + SpecialistProfile + ReviewCard |
| Phase 3 | ✅ Done | ReviewSubmit (5-star selector, body input, submit), FavoritesScreen (My Village grid), Saved button in ExpertsHome header |
| Phase 4 | ✅ Done | BookingScreen (service selector, date/time picker), PaymentScreen (Stripe PaymentSheet), BookingConfirmScreen (animated success), create-payment-intent Edge Function, appointmentsApi |
| Phase 5 | ✅ Done | MessagingScreen (thread UI, optimistic send, 30s polling), messagesApi, Message button in SpecialistProfile action bar, telehealth deeplink in header |
| Phase 6 | ✅ Done | All 7 AI Edge Functions (ai-review-summary, ai-translate, ai-profile-qa, ai-followup-questions, ai-match, ai-triage, appointment-reminder), twilio-sms, calendly-webhook, aiApi client, AIAssistantModal (Q&A + follow-up questions), wired into SpecialistProfileScreen AI rail |
| Phase 7 | ✅ Done | npi-verify Edge Function (NPI Registry API + 30-day cache), admin-approve-specialist Edge Function (service role + Twilio SMS notify), 013_v1_admin_approval.sql migration (admin_approved flag, specialist_admin_queue view, RLS update), NPI badge on SpecialistCard + SpecialistProfileScreen |
| Phase 8 | ✅ Done | pg_cron migration (014_v1_cron_jobs.sql: appointment-reminders every 15min, review-summary-refresh daily 3am ET), push-notify Edge Function (OneSignal), refresh-stale-summaries Edge Function, appointment-reminder updated to send push + SMS, useOneSignal hook (device registration + external_id linking), wired into RootNavigator |
| Phase 9 | ✅ Done | SkeletonLoader (SkeletonBox, SpecialistCardSkeleton, ProfileHeroSkeleton, ReviewCardSkeleton, ExpertsListSkeleton), FlashList upgrade on ExpertsHomeScreen, skeleton states on SpecialistProfileScreen, RLS audit migration (015_v1_rls_audit.sql), accessibilityLabel + accessibilityRole + accessibilityState on all primary CTAs |
| Phase 10 | ✅ Done | Sentry (initSentry + PII scrub), useAnalytics (Sentry breadcrumb + screen tracking via NavigationContainer onStateChange), EAS build profiles (dev/preview/prod), app.json updated (Sentry + OneSignal plugins + OTA updates), .env.example updated, 016_v1_prod_readiness.sql (pg_stat_statements, indexes, statement_timeout, admin_audit_log, soft-delete users, feature_flags) |
