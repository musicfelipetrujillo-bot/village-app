# Billy Capability Coverage — Wave 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Push Billy's coverage from 15 (6 live + 9 Wave-1 `code`) toward ~30 by wiring the next-highest-value capabilities: the remaining navigable route-to deep-links, and the three most-used do-it writes (save/unsave, draft day sheet, log a milk-stash bag).

**Architecture:** Same two patterns Wave 1 established. Route-to = add keys to the `navigate` tool's `NAV_TARGETS` (edge) + `NAV_ROUTES` (client) allowlists — no new tool files. Do-it writes = one new `tools/<name>.ts` per capability, copying the `log_baby_event` template (resolve owner server-side, RLS-scoped insert/RPC via the user JWT, confirm back).

**Tech Stack:** Deno edge function (`app-help-chat` tool registry), React Native client, Supabase RLS. Verification bar (unchanged from Wave 1): `pnpm type-check` + `pnpm lint` + live eval; **no deno compiler locally** — edge `.ts` correctness rests on schema-verification + review + the deploy compile.

**Prereq / gate:** **Track B (do-writes) must land AFTER Wave 1 deploys clean** (`supabase functions deploy app-help-chat` succeeds + the 9 Wave-1 evals pass). Track B shares the registry + write pattern that has not yet been compiled by deno; do not stack more write tools on an unvalidated foundation. **Track A (route deep-links) may proceed anytime** — it is a near-trivial allowlist extension.

**Source of truth:** `docs/BILLY_CAPABILITY_MAP.md` (the `route`/`do` rows still marked `no`). **Spec:** `docs/superpowers/specs/2026-07-22-billy-capability-coverage-design.md`. **Wave 1 plan (patterns to copy):** `docs/superpowers/plans/2026-07-22-billy-capability-coverage.md`.

**git hygiene (critical):** the working tree carries a large uncommitted logo/color rebrand (~100+ files). NEVER `git add -A`/`git add .` — stage only this task's files by explicit path, then `git show --stat HEAD` to confirm.

---

## Track A — Route deep-link batch (one task, do first)

Extend the generic `navigate` tool to cover the navigable route rows. No new tool files.

**Files:**
- Modify: `supabase/functions/app-help-chat/tools/navigate.ts` (grow `NAV_TARGETS` + description)
- Modify: `apps/mobile/src/screens/help/AIHelpChatScreen.tsx` (grow `NAV_ROUTES`)
- Modify: `docs/BILLY_CAPABILITY_MAP.md` + `docs/BILLY_EVALS.md` (flip rows to `code`)

### Task A1: add the navigable route destinations

- [ ] **Step 1: Decide the new keys.** Add these to `NAV_TARGETS` (edge) and `NAV_ROUTES` (client). Each `intended screen` MUST be verified against the real navigator before committing (Wave 1 found the Me tab is registered `Profile`, not `Me`):

| new NAV key | capability (eval id) | intended tab / screen (VERIFY) |
|-------------|----------------------|--------------------------------|
| `write_review` | E-write-specialist-review | Experts / ReviewSubmit |
| `message_specialist` | E-message-specialist | Experts / Messaging (thread compose) |
| `create_milk_listing` | E-create-milk-listing | Milk / CreateListing |
| `milk_messages` | E-send-milk-message | Milk / MilkMessageThreads |
| `vault_create_listing` | E-vault-create-listing | Milk / (milk-vault create-listing screen) |
| `gear_status` | E-update-gear-status | Gear / MyListings |
| `gear_messages` | E-send-gear-message | Gear / GearMessageThreads |
| `report_gear` | E-report-gear-listing | Gear / GearListingDetail (report modal lives here) |
| `community_post` | E-post-room-message | (Community/Connect nav — VERIFY the tab is enabled; the Connect tab is hidden per standing rule, so if there is no reachable route, DO NOT add this key — leave E-post-room-message as `no` and note it) |

- [ ] **Step 2: Verify every screen name.** For each row, grep the navigators for the exact `Stack.Screen name=` and tab name; correct the map to reality or drop the key if the destination doesn't exist / isn't reachable:

```bash
cd "village-app" && grep -rhoE "name=\"[A-Za-z]+\"" apps/mobile/src/navigation apps/mobile/src/**/*Navigator.tsx | sort -u
```
Confirm `ReviewSubmit`, `Messaging`, `CreateListing` (Milk vs Gear — there may be two; scope by navigator file), `MilkMessageThreads`, `MyListings`, `GearMessageThreads`, `GearListingDetail` exist. **Community: check whether the Connect tab is registered/enabled in `AppNavigator.tsx`** — per project rule it is hidden; if unreachable, omit `community_post` and record that in your report.

- [ ] **Step 3: Edit `tools/navigate.ts`.** Append the verified keys to the `NAV_TARGETS` array, and extend the tool `description` with a short clause per new destination (mirror the existing phrasing: "…write a review ('write_review'), message a provider ('message_specialist'), …"). Keep the handler unchanged.

- [ ] **Step 4: Edit `NAV_ROUTES` in `AIHelpChatScreen.tsx`.** Add one entry per verified key with the real `{ tab, screen }`. For `report_gear` (a modal on the detail screen) route to `GearListingDetail` and pass through `params` (the listing id, if Billy supplied one).

- [ ] **Step 5: Type-check + lint**

```bash
cd "village-app" && pnpm type-check && pnpm lint
```
Expected: PASS.

- [ ] **Step 6: Flip the map + evals.** In `docs/BILLY_CAPABILITY_MAP.md` set each wired row's `wired?` = `code`; in `docs/BILLY_EVALS.md` add the new eval ids to the "Wave 2" run-after-deploy note. Leave any omitted community row as `no` with a one-line reason.

- [ ] **Step 7: Commit (scoped)**

```bash
cd "village-app" && git add supabase/functions/app-help-chat/tools/navigate.ts apps/mobile/src/screens/help/AIHelpChatScreen.tsx docs/BILLY_CAPABILITY_MAP.md docs/BILLY_EVALS.md
git commit -m "feat: Billy navigate — wave 2 route deep-links"
git show --stat HEAD   # confirm only these 4 files
```

---

## Track B — Do-it write tools (after Wave 1 deploys clean)

Three new write tools, each copying the `log_baby_event` template exactly (see `tools/logBabyEvent.ts`): resolve the owner server-side from the JWT-scoped client, RLS-scoped write, return `{ ok, … }` or a friendly error; `tier: 'do'`; register in `registry.ts`; commit the tool file + registry only.

**Per-tool required discipline (Wave 1 proved this is not optional):** BEFORE writing insert code, read the backing `apps/mobile/src/api/<module>.ts` AND the migration that creates the table, and confirm every column name + CHECK constraint + NOT-NULL default. Wave 1's `log_baby_event` had to change `source:'villie_chat'` → `'note'` because the CHECK was `('manual','note')`. Do the same verification here; do not trust column names from this plan without checking.

### Task B1: `save_item` — save/unsave a specialist / donor / gear listing

**Files:** Create `supabase/functions/app-help-chat/tools/saveItem.ts`; Modify `tools/registry.ts`.

- [ ] **Step 1: Discover the real save/unsave calls.** Saving is per-vertical, not a single table. Read `apps/mobile/src/api/specialists.ts`, `milk.ts`, `gear.ts` (and `saved.ts`) to find the exact save/unsave operations. Expected shapes to CONFIRM (fix to reality): specialist → `favorites` insert/delete; donor → a `saved_donor*` table; gear → `gear_saved_listings` insert/delete (there is a `bump_gear_save_count` trigger). Note the exact table/column/RPC for each of the three types.

- [ ] **Step 2: Write `tools/saveItem.ts`** mirroring `logBabyEvent.ts`. Schema:
```
name: 'save_item'
input: { item_type: enum['specialist','donor','gear'], item_id: string(uuid), unsave?: boolean }
```
Handler: resolve `user_id` from `ctx.supabase.auth.getUser()`; branch on `item_type` to the verified insert (or delete when `unsave`); return `{ ok:true, saved:!unsave, item_type }` or `{ error }`. If `item_id` is missing, return `{ error:'need_item_id', message:'Ask her which one — you need its id from a prior search result.' }`. `tier:'do'`. Description: "Save (or unsave) something she found — a specialist, milk donor, or gear listing — to her saved list. Only call with a real item id from a search result; if you don't have one, ask her which result she means."

- [ ] **Step 3: Register** in `registry.ts` (import + append `saveItem`).
- [ ] **Step 4: Verify** the tables/columns exist (grep migrations for `favorites`, the donor-saved table, `gear_saved_listings`); fix code to match. Then `pnpm type-check && pnpm lint` → PASS.
- [ ] **Step 5: Commit (scoped)** `git add supabase/functions/app-help-chat/tools/saveItem.ts supabase/functions/app-help-chat/tools/registry.ts && git commit -m "feat: Billy save_item write tool"` then `git show --stat HEAD` (expect 2 files).

### Task B2: `draft_day_sheet` — auto-draft a caregiver day sheet

**Files:** Create `supabase/functions/app-help-chat/tools/draftDaySheet.ts`; Modify `tools/registry.ts`.

- [ ] **Step 1: Read `apps/mobile/src/api/daySheets.ts`** + the day-sheet migration (project memory: migration 102 + edge fn `day-sheet-page`). Find the create/draft call (RPC or insert) and its required params (baby_profile_id, date, generated schedule source). The day sheet is auto-drafted from logged feeds/naps — confirm whether drafting is an RPC that composes server-side or a client-side compose + insert. If it is an edge function, the tool should `functions.invoke` it with the user JWT.
- [ ] **Step 2: Write `tools/draftDaySheet.ts`** mirroring the template. Schema: `name:'draft_day_sheet'`, input `{ date?: string (YYYY-MM-DD, default tomorrow) }`. Handler: resolve baby_profile_id (reuse the `resolveBaby` pattern from `logBabyEvent.ts`), call the verified create/draft path, return `{ ok:true, day_sheet_id, date }` or `{ error }`. `tier:'do'`. Description: "Draft a shareable caregiver day sheet (nanny/grandparent handoff) from her logged feeds & naps for a given day. After it returns ok, tell her it's a draft she can review, edit, and share from the Day Sheet screen."
- [ ] **Step 3: Register**, **Step 4: verify + type-check + lint**, **Step 5: commit (scoped, 2 files)** — same shape as B1.

### Task B3: `log_milk_stash` — add a pumped-milk bag to the Milk Vault

**Files:** Create `supabase/functions/app-help-chat/tools/logMilkStash.ts`; Modify `tools/registry.ts`.

- [ ] **Step 1: Read `apps/mobile/src/api/milkVault.ts`** + migration `099_v6_milk_vault.sql`. Find the "add a bag to the freezer stash" insert (table `milk_vault_*`) and its columns (ounces, pumped_at/date, location/freezer, etc.) + CHECK constraints. **This is a personal-stash log (tier `do`), NOT the public sell/donate listing (that's route, already in the map).** Confirm the private-stash table vs the listing table — do not write to the listing table.
- [ ] **Step 2: Write `tools/logMilkStash.ts`** mirroring the template. Schema: `name:'log_milk_stash'`, input `{ ounces: number, pumped_at?: string, freezer?: string }`. Handler: resolve `user_id`; insert the bag with verified columns; return `{ ok:true, ounces }` or `{ error }`. `tier:'do'`. Description: "Add a pumped-milk bag to HER personal freezer stash (the Milk Vault). Use when she says she pumped/stored milk, e.g. 'add 5 oz to my stash'. This is her private stash, not a public listing. Confirm the total after."
- [ ] **Step 3: Register**, **Step 4: verify + type-check + lint**, **Step 5: commit (scoped, 2 files)**.

### Task B4: close Wave 2 on the scoreboard

- [ ] Flip `docs/BILLY_CAPABILITY_MAP.md` rows for `save-item`/`draft-day-sheet` (and the specific save eval ids) + the milk-stash row to `code`. Add their eval ids to the Wave-2 run-after-deploy note in `docs/BILLY_EVALS.md`. Commit both docs (scoped).

---

## After Wave 2

Remaining `no` rows (mostly the long tail of reads, timer start/stop, notes/parse-note reuse, and the in-flow legal-acceptance gates) → Wave 3, same two patterns. The parse-note capability (`E-parse-note`) is worth prioritizing there: the `playbook-parse-note` edge fn already exists, so a `jot_note` tool can `functions.invoke` it rather than re-implement parsing.

## Self-Review (author)
- **Coverage:** Track A wires the 8–9 navigable route rows (map lines 61/69/83/88/103/111/117/121/142); the 4 legal-acceptance + questionnaire/diet/med rows are correctly NOT given standalone nav (in-flow gates / reached via `become_donor`/`donor_profile_edit`). Track B wires the 3 highest-use do-writes. ✓
- **Placeholders:** Track B tasks intentionally instruct "read the module + migration, then write the insert" rather than hardcoding inserts — this is deliberate (Wave 1 proved guessed columns break at the CHECK constraint), not a placeholder. Screen names in Track A are marked VERIFY for the same reason.
- **Type consistency:** all new tools are `ToolDef` (`tier:'do'`) appended to `REGISTRY`; `save_item`/`draft_day_sheet`/`log_milk_stash` reuse `logBabyEvent`'s `resolveBaby`/auth pattern. Route keys are added to both `NAV_TARGETS` (edge) and `NAV_ROUTES` (client) — same-key discipline as Wave 1.
- **Gate honesty:** Track B explicitly gated on Wave 1 deploying clean, since deno can't compile the edge code locally.
