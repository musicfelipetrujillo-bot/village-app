# Villie — Security & Correctness Audit

**Date:** 2026-09-04
**Type:** Review, then remediation, then deployment. Findings came from a read-only multi-agent review; fixes were written on `sec/audit-fixes`, merged to `main`, and **DEPLOYED TO PRODUCTION on 2026-09-07**.

**Deployment record (2026-09-07)** — migrations `133`–`136` applied (`supabase migration list` confirms local/remote parity); `pro-entitlement-reconcile` deployed alone first so its `verify_jwt` pin shipped with its code; the remaining 47 edge functions deployed after. Deployed from a tree fast-forwarded to `origin/main` — the branch tip was 23 files BEHIND main, and deploying from it would have reverted the voice-dictation, parks and `app-help-chat` work, since `functions deploy` bundles the working tree. **Verified live:** `twilio-sms`, `push-notify`, `geocode-zip`, `daycares-nearby`, `home-feed-curator`, `events-harvest` and `ai-translate` all return **401** to the anon key that ships in the mobile bundle. Profile edits re-verified in-app after `133`.
**Scope:** All three codebases — `village-app` (76 edge functions, 131 migrations, ~2.9k mobile TS files), `village-app-pro-gate` worktree, `village-website`.
**Method:** Seven parallel review agents (edge-function auth, RLS/migrations, mobile client security, mobile correctness, monorepo architecture, pro-gate, website), each finding then re-verified by hand against `origin/main`.

**Prior art:** `docs/audits/security-privacy-2026-08-14.md` · `docs/audits/appsec-2026-07-07.md` · `docs/audits/privacy-minimization-2026-07-09.md`

---

## Method note — stale-checkout distortion (read this before trusting any re-run)

The review ran against the `village-app` working checkout, which sits on `feat/billy-capability-coverage`, **106 commits behind `origin/main`**. Several "findings" were artifacts of that gap and do **not** apply to main. Every item below was re-checked against `origin/main` before being written down.

Cleared on re-check — already fixed on main by the 2026-08-14 remediation or later:

| Reported | Reality on `origin/main` |
|---|---|
| 6 functions trust an unsigned `role` claim | 5 of 6 use `_shared/service-role.ts`. Only `pro-entitlement-reconcile` still decodes unverified. |
| `config.toml` has no `verify_jwt` pins | 75 functions pinned (301-line file). The 45-line unpinned version exists only on the stale branch. |
| `create-payment-intent` charges a client-supplied amount | Priced server-side from `specialist_services`. (The **currency** half was still live — fixed here, see R-4.) |
| Baby-tracker stale cached `_userId` drops all logs after account switch | Fixed on main. |
| Zero test infrastructure | `vitest` is wired up (`apps/mobile`), 1 test file. Coverage is still ~nil, but the runner exists. |
| Waitlist emails may be anon-readable | Not readable. `100_waitlist.sql` grants anon `INSERT` only, no SELECT. |

**Takeaway for future audits:** review from `origin/main`, or state the base commit in the report. A 106-commit gap made a remediated class of bug look systemic and live.

---

## Remediation log (2026-09-04, branch `sec/audit-fixes` — NOT yet applied)

| # | Finding | Severity | Fix |
|---|---|---|---|
| R-1 | Any user could self-grant `is_pro` / `is_clinical_reviewer` / `is_event_reviewer` via `PATCH /users?id=eq.<self>` | **Critical** | `133_security_users_column_scope_update.sql` — table-wide UPDATE replaced with an 11-column allowlist |
| R-2 | `twilio-sms` + `push-notify` invocable by any anon-key holder | **Critical** | `isServiceRoleRequest(…, { gatewayVerifiesJwt: true })` added to both |
| R-3 | Specialist admin-approval boundary was a no-op | **High** | `134_security_specialists_drop_permissive_select.sql` — drops `specialists_select_all` |
| R-4 | `create-payment-intent` accepted a client-supplied `currency` | **High** | Pinned to `'usd'` server-side |
| R-5 | Fabricated clinician byline over a Mux test clip shown as this week's guidance | **High** | Placeholder removed; slot hides; `.catch` fails closed |
| R-6 | Free bookings never persisted — confirmation shown for a non-existent appointment | **High** | Persist before navigating; retry-in-place on failure |

### R-1 — self-granted privileges (critical)

`users_update_own` (`002_v1_rls_policies.sql:18`) scoped which *row* a caller may update but never which *columns*, and Supabase's default table-wide `GRANT UPDATE` made every column writable by its owner. The three privilege flags live on that same self-writable row (`043:20`, `046:35`, `110:20`).

Reachable consequences: publish arbitrary unreviewed AI health content to every user via `approve_content_row` (042:299); control the public events feed; free villie Pro.

**This also explains why migration 110's Pro gate looked airtight but wasn't.** The SQL enforcement in 110 is correct — playback ids nulled, RLS hides locked rows. The *flag it reads* was self-writable, so the gate was bypassed one level up. R-1 is what makes 110 actually hold.

Follows the `095` pattern, including its hard-won lesson: a column-level `REVOKE` is a **no-op** while a table-wide grant exists.

Verified by execution against Postgres 16 (stub schema, real grants): escalation `UPDATE` → `permission denied`; email takeover → `permission denied`; ordinary profile edit → succeeds; SELECT unaffected, so clients still read `is_pro`.

### R-2 — SMS and push open relays (critical)

Both functions carried an "Auth: service role only" header comment and performed **no authorization at all**. `verify_jwt = true` is not a substitute: it proves only that the bearer is signed by this project's JWT secret, and **the anon publishable key is exactly such a token** — it ships in the mobile bundle and on the marketing site.

This is the load-bearing insight of the whole review: it means every "server-only" function without an in-code gate is effectively internet-public *regardless of gateway settings*. Reachable: arbitrary SMS from Villie's Twilio number (toll fraud + smishing from a trusted number); arbitrary push to any user list, with caller-supplied `bypass_prefs: true` overriding notification preferences **and** quiet hours.

All 9 existing callers were verified to send `SUPABASE_SERVICE_ROLE_KEY` before gating, so no caller breaks.

### R-3 — specialist approval no-op (high)

`013_v1_admin_approval.sql:49` dropped `"Specialists are publicly readable"`; the policy actually created in `002:22` was named `specialists_select_all`. `IF EXISTS` swallowed the miss and the permissive `USING (true)` survived ~120 migrations. `015_v1_rls_audit.sql:3` asserts the control was in place, which is why it went unnoticed.

Exposed to **anon**: `phone`, `address_line1`, `stripe_account_id`, and `admin_rejection_reason` for pending and rejected practitioners.

Verified before dropping: the website never reads the table; 7 of 8 edge functions use service-role; the 8th (`create-payment-intent`) runs under the caller's JWT and retains `authenticated` read of approved rows.

### R-5 — fabricated clinical attribution (high)

`ManualScrollV3.tsx` substituted a placeholder titled "What to expect this week", attributed to **"Dr. Priya Nair, MD · pediatrician · villie"** — a person who does not exist — playing a public Mux sample clip.

Not an edge case: `get_manual_week_intro` filters `locale` exactly with no `en` fallback (`110:184-187`), so for a Spanish-speaking user under the English-first Pro launch **no week ever resolves** — she got the fake pediatrician on all 52 weeks, as did paying Pro subscribers in place of purchased content. `docs/PRO_VIDEO_LAUNCH_RUNBOOK.md`, added in the same commit as the locale-scope migration, documents the intended behaviour as "she gets no week-intro slot at all (it hides…)". Code contradicted its own runbook.

The `.catch` now also fails closed: it previously substituted a playable object carrying no `is_locked`, so `openWeekIntro` skipped the paywall — a transient network error handed free-tier users a playable card.

---

## Closed: `095` deferred item — donor `lat`/`lng` (no action)

`095_security_milk_donor_pii_column_revoke.sql:37-40` deferred revoking `milk_donor_profiles.lat/lng` pending verification. **Closing it: no change needed.**

- Coordinates are **ZIP-centroid derived**, not house-level geocodes (confirmed by the product owner, 2026-09-04). A ZIP centroid is coarser than the `zip_code` and `city` columns already selectable on the same row, so revoking buys no privacy.
- Independently, nothing in the repo writes these columns — no mobile code, no SQL, no edge function, no seed. They are populated out-of-band.
- A blanket revoke would be actively harmful: column grants are role-wide, not row-aware, so it would break a donor reading **her own** profile (`getMyDonorProfile`) and `listSavedDonors`. `DonorMapScreen` is unaffected either way — it reads through the `search_donors_near` SECURITY DEFINER RPC, which ignores column grants.

**Revisit only if** donor coordinates ever become precise geocodes. At that point the fix is a separate coarsened column plus jitter in `search_donors_near` — not a grant change.

---

---

## Remediation log, batch 2 (2026-09-05) — the gateless-function class

Re-derived the gateless set against this branch rather than trusting the review's list (which came from the stale checkout). Detecting *any* authorization signal — shared gate, `auth.getUser`, key compare, signature verification, 401 path — leaves **13 functions with no authorization of any kind**. Eleven are now gated; two are correctly public and stay that way.

**Service-role gate (8).** Every caller verified to send `SUPABASE_SERVICE_ROLE_KEY` before gating — `pg_cron` via `current_setting('app.service_role_key')` (`014_v1_cron_jobs.sql:18`), GH Actions via the repo secret (`supabase-crons.yml:168`). None break.

| Function | Was reachable as | Caller |
|---|---|---|
| `appointment-reminder` | drive the SMS+push sweep, write `twilio_reminder_sent` | pg_cron /15m |
| `events-geocode` | burn the Google Geocoding quota, write event coords | pg_cron |
| `events-ingest-ics` | trigger partner-feed ingest; fetches remote ICS, writes events | pg_cron |
| `refresh-stale-summaries` | one Anthropic call per stale specialist — an open tap on the bill | pg_cron daily |
| `week-nudge-notify` | push fan-out with attacker-tuned windows; `push_sends` pollution | GH Actions hourly |
| `trending-publish-notify` | Buzz publish push fan-out to the user base | pg_cron |
| `npi-verify` | **flip `specialists.npi_verified = true`** — forge the credential badge | **none in-repo** |
| `home-feed-curator` (batch) | fleet-wide Haiku fan-out on demand | pg_cron + mobile |

`refresh-stale-summaries` did not even accept the `Request` (`Deno.serve(async () => {`), so there was nothing to authorize against. `npi-verify` has no caller anywhere — deployed, unreferenced, and writing a trust signal.

**`home-feed-curator` — two-sided, because it has two legitimate callers.** `batch` is now service-role only. `single` accepts service-role for any `user_id`, otherwise requires a valid user JWT and forces the target to that caller's own id. This closes a live **IDOR**: `user_id` came straight off the body and was never checked, so any request could regenerate and overwrite another user's `home_feed_cache`. A body `user_id` naming someone else is refused (403) rather than silently retargeted, so a buggy client fails loudly.

**User-JWT gate (3)** — new `_shared/user-auth.ts`. `geocode-zip`, `daycares-nearby`, `gear-upc-lookup` are mobile-only and each calls a **billable third-party API** (Google Geocoding, Google Places, Go-UPC/UPCitemdb). They were open to anyone holding the anon key. They act on no specific user's records, so "a valid user is asking" is the right bar; the helper's docstring is explicit that anything touching a specific user's data must use `getCallerUserId` and compare ids instead.

**Deliberately left public (2):** `manual-og` (`verify_jwt = false`, no writes — renders OG meta for share links; gating breaks every link preview) and `specialist-invite-accept` (`verify_jwt = false` — the invitee is anon pre-signup; it is invite-token-gated by design). `specialist-invite-accept` still needs the **non-atomic invite claim** fixed — see Open #2.

`_shared/user-auth.ts` is the counterpart to `_shared/service-role.ts`, and directly addresses the audit's duplication finding: the JWT-resolution block was about to become its fourth copy.

**Verification:** all 12 changed/added function files parse clean (TypeScript parser). `deno check` could not run — Deno cannot resolve npm specifiers inside this pnpm workspace, and attempting it rewrites `package.json` (reverted). **Standing gap: edge functions still have no type-check in CI**, which is why a parse-only check is the best available signal here.

---

---

## Remediation log, batch 3 (2026-09-05) — the IDOR class

Batch 2 closed "no gate at all". This closes the subtler class: functions that checked *a* caller but not *which* caller, then read or wrote with the service-role client — so RLS never applied.

| Function | Unchecked input | Was reachable as | Rule now |
|---|---|---|---|
| `ai-daily-checkin` | `checkin_id` | AI reply derived from **another mother's free-text mental-health entry**, plus a PATCH of `ai_reply`/`crisis_flagged` onto her row | caller must own the check-in |
| `ai-match` | `user_id` | victim's `pregnancy_stage` + `insurance_provider` into an LLM, readable back out of the returned reasons | target forced to caller |
| `room-auto-match` | `user_id` | victim's stage/due-date/city into a "reason"; overwrote her `room_match_suggestions` | target forced to caller |
| `room-icebreaker` | `user_id` | overwrote any user's icebreaker for any room | target forced to caller |
| `room-ai-companion` | `message_id` | post an AI reply into a room you aren't in, under the trusted gold badge; **and exhaust another user's rate limit**, which is keyed to the sender | caller must be the message's sender |
| `ai-event-relevance`, `ai-perk-recommender`, `ai-gear-tip` | `user_id` | inference leak about another mother + a free Haiku endpoint | service-role only (sole caller is `home-feed-curator`) |
| `milk-donor-qa` | `donor_profile_id` | **owner-only health data** recited by an LLM | owner-scoped + visibility |
| `milk-trust-narrative` | `recipient_preferences` | poison a **globally-cached** donor trust narrative | input removed |

**Not-yours is answered with the same 404 as not-found** in `ai-daily-checkin`, `room-ai-companion`, `milk-donor-qa` and `milk-trust-narrative`. A distinct 403 would confirm that a given id exists — an enumeration oracle over mental-health records, private-room messages, and donor profiles.

**`milk-donor-qa` was a straight RLS bypass, not a design choice.** `milk_questionnaire_responses` is owner-only under RLS (`005_v2_milk_rls.sql:39-43`) — medications, conditions, alcohol, smoking. The function read it with the service-role client and injected it wholesale into the prompt, while the system prompt forbade revealing only "address or contact info". Any signed-in user could iterate donor ids and ask for the disclosures verbatim; the model answers from the data it was given, so no jailbreak was needed. The questionnaire is now fetched **only** when the caller is the donor herself — not fetched and discarded, since unused PHI in memory is still PHI. Everything else in the prompt (profile, badge level, diet flags) is authenticated-readable by design, so recipient-facing Q&A still works on the data it is permitted to use.

**`milk-trust-narrative` folded a per-viewer input into a shared artifact.** `recipient_preferences` was free text steering a narrative written to `milk_trust_badges.ai_trust_narrative` and served to every user for 24h — re-poisonable on expiry. The only caller (`DonorProfileScreen.tsx:118`) never passed it, so it was reachable only by hand-crafted request; deleting it costs nothing. The client parameter was removed too, rather than ignored server-side, so it can't look like a working feature.

**New shared helper:** `resolveTargetUser(req, bodyUserId, isService)` in `_shared/user-auth.ts` — service role may target anyone, a signed-in user only herself, and a body `user_id` naming someone else is **refused (403), not silently rewritten**. Quietly retargeting would hide a client bug behind an apparent success.

**Verification:** all 11 changed function files parse clean; `tsc --noEmit` clean for the mobile signature change. Same caveat as batch 2 — no Deno type-check available in this workspace.

---

---

## Remediation log, batch 4 (2026-09-05) — `events-harvest` SSRF

`body.probe.url` was fetched server-side from inside Supabase's network, on a function with **no authorization at all**. Two fixes, because the gate alone is not sufficient:

**1. Service-role gate.** Both legitimate callers are service role — the daily GH Actions cron (`supabase-crons.yml:57`, `BODY='{}'`) and ops running a probe by hand per `docs/OPS_RUNBOOK.md:75`. No mobile code calls it. This alone removes the internet-facing SSRF *and* stops anyone triggering the full Haiku harvest fan-out over every registered feed.

**2. URL guard with per-hop revalidation.** Still needed after the gate, because `fetchDirect` used `redirect: 'follow'` — so a *registered feed* URL that later redirects somewhere internal is the same hazard, and no gate covers it. Validating only the first hop is the classic way an SSRF check gets bypassed. `fetchDirect` now follows redirects manually (bounded at 5), revalidating **every hop**; `fetchRendered` validates before interpolating into the `r.jina.ai/` proxy path. Blocked: non-http(s) schemes, loopback, RFC1918, CGNAT, link-local (`169.254.*` — cloud metadata), IPv6 loopback/unique-local/link-local. The probe branch validates up front and returns a 400 naming the reason, because both fetch paths swallow errors into "no events extracted" — which is exactly the signal ops uses to decide *not* to register a feed.

**Verified by execution**, against the shipped code extracted from the file (not a reimplementation):
- Guard unit test: 15 hostile URLs blocked, 5 legitimate ones allowed — including boundary cases `172.32.0.1` and `100.128.0.1`, which sit just outside RFC1918/CGNAT and must *not* be blocked.
- Live redirect test under Deno: a normal redirect chain still resolves to page content (feeds don't break); a "public" URL redirecting to `169.254.169.254` throws `url_host_blocked` at the hop; a redirect loop terminates with `too_many_redirects`.
- Confirmed first that Deno's `redirect: 'manual'` exposes the real status and `Location` rather than an opaque response — if it did not, hop-following would have silently broken every redirecting feed, and per the runbook a zero-yield feed auto-deactivates after 3 days.

**Known limitation, stated rather than papered over:** this is a hostname/IP-literal check, not a resolved-address check. A hostname that *resolves* to a private address still passes, and DNS rebinding between check and fetch is not defeated. Closing that needs resolve-then-connect-by-IP with a pinned Host header, which is more machinery than this ops tool warrants now that it is service-role gated. The gate is the primary control; the guard is defence in depth.

---

## Correction — `specialist-invite-accept` was already fixed (5th stale finding)

The review reported a non-atomic invite claim (H-5). **It does not exist on `main`.** The 2026-08-14 remediation (M-1) already replaced the SELECT-then-work-then-mark pattern with a single conditional `UPDATE` (`specialist-invite-accept/index.ts:126-134`) that filters on `used_at IS NULL`, `revoked_at IS NULL` and `expires_at > now()`. Postgres locks the row, so exactly one concurrent caller can flip `used_at`; everyone else gets zero rows and is rejected before any work happens. Failure paths hand the claim back (`releaseClaim`), and a failed specialists insert also rolls back the auth user via `admin.deleteUser`. No fix was needed or made.

That is the **fifth** finding invalidated by the stale checkout, reinforcing the method note at the top of this document.

---

---

## Remediation log, batch 5 (2026-09-05) — the LLM/vision proxies

21 more functions gated. Every caller was traced first; none break.

**Service-role (9)** — cron/internal only, each caller verified to send the service key:
`ai-review-summary` (via `refresh-stale-summaries`), `ai-weekly-journey-fill`, `ai-milestone-explainer`, `ai-event-screen`, `room-weekly-summary` (pg_cron), `room-message-scan` (the AFTER-INSERT pg_net trigger, `027:127`), `ai-week-nudge-generate` (GH Actions), `trending-compliance-pass` (scheduled ops task), `gear-cpsc-recall-sync`.

**User-scoped (12)** — mobile-called, each already sends the user's JWT:
`ai-translate`, `ai-triage`, `ai-profile-qa`, `ai-followup-questions` (all via `api/ai.ts`'s `callEdgeFunction`), `gear-vision-identify`, `gear-cpsc-check`, `milk-vault-scan`, `milk-questionnaire-coach`, `room-alias-generate`, `agents-run` / `agents-triage` / `agents-health`.

### The false premise, stated in the code itself

This class existed because the codebase repeatedly recorded `verify_jwt` as authentication. Three examples, now corrected in place:

- `gear-cpsc-recall-sync`: *"the gateway rejects anything else with 401 before reaching here. No need for a redundant function-level check."* It does not — the anon key is a valid project JWT. The same comment records that the previous check was **deleted** because it "introduced an env-mismatch failure mode that broke this cron after the last key rotation" — which is the exact scenario `_shared/service-role.ts`'s two-mode gate was written for. Deleting authentication was never the fix. This function upserts the recall cache and runs `sweep_active_listings_for_recalls`, which auto-withdraws live listings and notifies sellers, so an open trigger was a content-integrity problem, not just a cost one.
- `agents-*`: *"Callers must be authenticated (Supabase verifies JWT by default)."*
- `room-alias-generate`: *"Auth: authenticated user JWT (verify_jwt: true default)."*
- `docs/OPS_RUNBOOK.md:329` asserts `trending-compliance-pass -> 401 (JWT-locked, GOOD)`. That smoke test only passes because it sends **no** bearer; an anon-key holder sailed straight through. A green check that proves the wrong thing.

### Two that were worse than "no gate"

- **`milk-questionnaire-coach`** had a *presence* check: any non-empty `Authorization` header passed. It authenticated nobody — it merely re-confirmed the gateway had let the request in.
- **`room-alias-generate`** base64-decoded the JWT and trusted `sub` **without verifying the signature** — the same defect `_shared/service-role.ts` was created to kill after the 2026-08-14 incident, applied to a user id instead of a role. `Bearer x.eyJzdWIiOiI8YW55LXV1aWQ+In0.x` impersonated any user, and the alias is persisted against that id, so a caller could seed or overwrite another member's anonymous identity in a room. Now uses `getCallerUserId`.

### Scope boundary — rate limiting is NOT implemented

The user gates stop **anonymous** abuse: the endpoints are no longer reachable with the bundled anon key. They do **not** stop a signed-in user looping a request in a script. Every user-gated function above remains an unmetered per-account model endpoint billed to Villie.

Doing it properly needs a shared counter (a `usage_ledger` keyed on `(user_id, function, window)`, checked in `_shared/`), which is a migration plus a new failure mode on every call site — deliberately not bundled into a security pass. `room-ai-companion` already has a per-user/room/hour limit via `ai_companion_mentions` and is the working model to generalise.

**Verification:** all 79 edge files parse clean; `tsc --noEmit` clean. Final sweep: **34** functions on the service-role gate, **21** user-scoped, and the only functions with no in-code authorization are the six that are correct by design — `manual-og` and `day-sheet-page` (public / share-token), `specialist-invite-accept` (invite token *is* the auth), `auth-google-exchange` (pre-session), and `admin-approve-specialist` / `admin-compliance-events` (exact service-key compare).

---

---

## Remediation log, batch 6 (2026-09-06) — `pro-entitlement-reconcile`, the last unverified decode

The function decoded the bearer's payload and returned `role === 'service_role'` **without verifying the signature**, justified in-code by *"The gateway verifies the JWT signature; we only need to know WHICH role signed in."* That premise was false here specifically: this was the **only** function absent from the `verify_jwt` registry, so nothing established the gateway was checking signatures at all. `Bearer x.eyJyb2xlIjoic2VydmljZV9yb2xlIn0.x` drove a fleet-wide entitlement sweep against the RevenueCat API.

Identical to the defect `_shared/service-role.ts` was written to kill on 2026-08-14; five of six functions were migrated then and this one was missed.

**Why `gatewayVerifiesJwt: true` and not `false`.** The original author's reason for reading the claim rather than comparing keys was legitimate and is documented in the helper: this project has more than one valid service-role key in circulation — the GH Actions secret and the runtime-injected key are both correctly signed but not byte-identical — so strict equality 401s this nightly cron. `false` (exact match only) would have broken the caller, which is precisely the breakage that caused authentication to be deleted elsewhere in this codebase. The helper is two-mode for exactly this: it accepts the injected key **or** an authentic `service_role` claim, keeping rotation tolerance without trusting an unverified token.

That mode is sound **only** because this change also adds `[functions.pro-entitlement-reconcile] verify_jwt = true`. **Code and config must ship together, and this function must never be deployed with `--no-verify-jwt`** — a note to that effect sits in `config.toml` beside the pin.

**The registry is now exactly complete: 75 functions, 75 entries, no gaps and no orphans.** That matters beyond this one fix — `_shared/service-role.ts` states that its `gatewayVerifiesJwt` flag "is checkable against that file in review". Until now that was not true for one function, and that function was the one still trusting an unverified claim.

### A verification lesson worth keeping

Two functions in earlier batches (`gear-cpsc-recall-sync`, and this one) ended up **calling the shared gate without importing it** — a scripted edit silently failed. Both still passed the TypeScript **parse** check, because parsing does not resolve identifiers. In production each would have thrown `isServiceRoleRequest is not defined` on every invocation, taking down two crons.

Parse-clean is not correct-clean. There is now a symbol-resolution check — for every function, if it *calls* a shared gate it must *import* it from the right module (and no dead imports) — run across all call sites: **56 verified**. Until edge functions get a real type-check in CI, that check should be re-run after any bulk edit.

---

---

## Remediation log, batch 7 (2026-09-07) — per-user quotas

Closes the batch-5 scope boundary. Gating answered *who is calling*; nothing answered *how much they may have*, so every user-scoped model endpoint was an unmetered per-account endpoint billed to Villie. **23 functions** now carry a per-user hourly budget.

**`135_ai_rate_limits.sql`** — an `ai_rate_limits` counter table plus `consume_ai_quota(user_id, fn, limit, window_seconds)`.

- **The decision lives in SQL because it must be atomic.** A read-then-write in the edge function is a TOCTOU race: N concurrent requests all read the same count and all pass. The RPC decides and increments in ONE `INSERT … ON CONFLICT DO UPDATE … WHERE count < limit RETURNING` — Postgres locks the row, so concurrent callers serialise and the overflow gets zero rows back.
- **Fixed window, not sliding.** Sliding needs every timestamp retained and an aggregate per check; fixed needs one row and one upsert. The cost is the standard boundary burst, which is a fine trade for a cost control.
- **Deny-all table shape** (RLS on, no policies) — the pattern already used by `events_partner_feeds` / `specialist_invites` / `push_sends`. `EXECUTE` is granted to `service_role` only: a client that could call this could reset its own quota.
- `p_user_id` is passed in rather than read from `auth.uid()` because the invoker is `service_role`. That is safe **only** because the id was already established from a verified JWT via `getCallerUserId`.

**Verified by execution** against Postgres 16 (not asserted):

| Property | Result |
|---|---|
| Sequential limit | limit 2 → `t/1`, `t/0`, `f/0` + retry_after |
| **Atomicity** | **40 concurrent callers, limit 10 → exactly 10 allowed, 30 denied, stored counter = 10** |
| Per-function isolation | separate budget per `fn` key |
| Window rollover | 1s window: exhausted, then allowed again after expiry |
| Self-cleanup | stale buckets pruned, 1 row retained |
| Deny-all | `authenticated` gets `permission denied` on both table and function |

### Placement rules (the part that is easy to get wrong)

- **Default: immediately after auth, before any model call.** Uniform placement means it covers every downstream branch; deferring it deeper per-function risks a path that skips it. The side effect is that malformed requests also consume budget — correct for abuse control, and harmless for a well-behaved client.
- **Exception — `milk-trust-narrative`: below the 24h cache lookup.** This is called on *every* `DonorProfileScreen` view and the overwhelming majority are cache hits costing nothing. Charging them would have rate-limited a mother for simply browsing donors — a 20/hr ceiling reached after 20 profile views while Villie spent nothing. The budget must bound the expensive operation, not the cheap one.
- **Service-role paths are never charged.** `ai-daily-checkin` and `home-feed-curator` both have cron callers that legitimately run across many users; only the user path consumes quota.
- **`room-ai-companion` is deliberately excluded** — it already enforces 3/user/room/hour off `ai_companion_mentions`. It was the model for this design, not a target of it.

### Fail-open, deliberately — and the asymmetry with the auth gates

`consumeQuota` returns `allowed: true` and logs loudly if the ledger itself errors (DB unreachable, migration not yet applied). This is a **cost** control, not a safety control: the alternative is that a transient database problem silently blocks a postpartum mother from her check-in reply. An attacker cannot usefully induce that state — they would have to break the database, at which point the quota is not what is protecting you.

Note the contrast with `service-role.ts`, which fails **closed**. Different question, different default: "may this caller act at all" must fail closed; "have they had too much" may fail open.

### Client-side

A 429 would otherwise have surfaced the raw wire string `rate_limited` in an `Alert` — meaningless to a user and alarming at 3am. `api/ai.ts`'s shared `callEdgeFunction` now maps 429 to a translated message using `Retry-After`, so it says *when* rather than just *no*. EN/ES keys added under `errors.rateLimited`; parity re-verified (2,134 each, nothing removed).

**Limits** live in one table in `_shared/rate-limit.ts`. Sizing principle: comfortably above the busiest real session, low enough to stop a script. Vision endpoints are tightest (20/hr); `milk-questionnaire-coach` is deliberately generous (80/hr) because the donor questionnaire coaches each of ~12 answers and she may revise. A function with no entry logs a warning rather than silently running uncapped.

**Verification:** all 100 edge files parse clean; `tsc --noEmit` clean; the symbol-resolution check passes at **101 call sites** with no missing or dead imports; and a coverage audit confirms 23 limits ↔ 23 wired functions with matching keys and no orphans in either direction.

---

## Remediation log, batch 8 (2026-09-07) — room-message integrity (Connect pre-launch gate)

`136_security_room_message_integrity.sql`. Connect is hidden in `AppNavigator`, so no app user is exposed today — but RLS has been enforcing since migration 040 and `room_messages` is reachable through PostgREST with any authenticated session, so the surface is live on hosted. **This was the gate blocking Connect from ever being un-hidden.**

Three vectors, not the two originally reported:

1. **INSERT never constrained `message_type`.** `messages_insert_members` (`007:52-61`) checked sender and membership only, while the column accepts `system | ai_companion | expert`. `scan_room_message_async()` (`027:115-120`) treats those three as trusted and marks them `clear` **inline, without ever calling the classifier**. So a member could post as an `expert` and skip the Haiku crisis + moderation scan entirely — in a maternal-mental-health chat whose safety model is that every user message is screened for self-harm before anyone sees it. The same message renders under the gold "✨ Villie · AI companion" badge or as an official system card, so it is also clinical-authority impersonation to an audience of postpartum mothers.

2. **INSERT never constrained `ai_scan_status`** — found while reading the trigger, *not* in the original finding. The trigger's first statement is `IF NEW.ai_scan_status <> 'pending' THEN RETURN NEW` (`027:111-113`), an idempotency guard. Since the policy never pinned the column, a member could insert an ordinary `user` message with `ai_scan_status = 'clear'` and the trigger returned immediately. No scan, instantly visible (the read policy admits `clear`), **and no impersonation required**. This was the simplest bypass of the three and would have survived a fix that only addressed `message_type`.

3. **UPDATE was row-scoped, never column-scoped.** `messages_update_own` (`007:63-66`) pinned `sender_user_id` in USING and WITH CHECK — which *row*, never which *column*. A sender could flip her own `ai_scan_status` to `clear` (releasing a message the classifier had held as `flagged` or routed as `crisis`), rewrite `body` after the scan cleared it (the trigger fires on INSERT only — scan-then-swap), or flip `message_type` post-insert to reach vector 1 by another door.

**Fix.** INSERT now additionally requires `message_type = 'user'` and `ai_scan_status = 'pending'`. The UPDATE policy is **dropped** rather than narrowed: no client code updates this table — `api/community.ts` touches `room_messages` exactly twice, an INSERT of `{room_id, sender_user_id, body}` at `:171-173` and a SELECT of `ai_scan_status` at `:246-247`. There is no edit- or delete-message feature, so nothing is granted that isn't used. With RLS on and no UPDATE policy, every member update is denied.

**Verified by execution** — the vulnerability was *reproduced first* against the original 007 policies, then the migration applied and the identical attacks re-run:

| Attack (as a room member) | Before | After |
|---|---|---|
| insert `message_type='expert'` | **succeeded** | RLS violation |
| insert `message_type='ai_companion'` | **succeeded** | RLS violation |
| insert `ai_scan_status='clear'` | **succeeded** | RLS violation |
| self-clear own pending message | **cleared 3 rows** | 0 rows affected |
| normal user message | ok | ok (`user` / `pending`) |

And the legitimate trusted-message producers are untouched — as `service_role`: the `ai_companion` pre-cleared insert, the `system` weekly digest, and `room-message-scan`'s verdict PATCH all still succeed.

*Harness note:* the first run showed every insert failing even **before** the fix, which would have been a false pass. Two stub defects caused it — no `USAGE` on the `auth` schema (so `auth.uid()` raised inside the policy and surfaced as a violation), and no SELECT policy (so `INSERT … RETURNING` was refused). Worth recording: a security test that fails closed for the wrong reason looks exactly like a security test that passes.

### If an edit/delete-message feature is ever built

Add a **new** policy scoped to only the columns it needs (`is_deleted` / `deleted_at`). Do not restore `messages_update_own` — and note that permitting `body` edits reintroduces scan-then-swap unless the classifier is re-run on UPDATE.

---

## Remediation log, batch 9 (2026-09-07) — a fix that wasn't, plus the website and deps

### 🔴 Migration 133 was a no-op against a PUBLIC grant

The most important finding of this batch is a defect in **this audit's own critical fix**.

`133` revoked table-wide UPDATE `from authenticated` and `from anon` — but not `from PUBLIC`. This project has been bitten by that exact trap twice already: `052` revoked FROM PUBLIC only and was a no-op because the grant was role-specific; `130` revoked FROM anon only and was a no-op because that grant was to PUBLIC. `131` wrote the rule down — **neither alone is sufficient** — and `docs/OPS_RUNBOOK.md` §9 records it as a standing trap.

Verified empirically rather than reasoned about: with `GRANT UPDATE ON users TO PUBLIC` in place, the shipped version of 133 left the ACL at `=rw/postgres` and `update users set is_pro = true` **still succeeded**. The migration read like a fix and enforced nothing.

Now `revoke update on public.users from public, anon, authenticated`. Re-verified under the same scenario: ACL drops to `=r/postgres` (SELECT retained, UPDATE gone), escalation denied, and all three real client write paths still succeed. Supabase's table defaults are role-specific so this is probably a no-op on the live DB — but a migration cannot see the live ACL, and the clause is free.

**The verification block was also wrong**, which is why the bug survived review: it asked `has_column_privilege`, which reported the column grants as correct while PUBLIC still held the table. It now inspects `pg_class.relacl` for a surviving table-wide `w` first.

Sibling check: `135` already revoked from public+anon+authenticated; `134` and `136` are policy-only and carry no grants.

**Lesson for future migrations here: a REVOKE is not verified until you have looked at the resulting ACL.**

### Website — security headers added (`village-website`, separate repo)

The site served **no** security headers at all, on a domain hosting the specialist onboarding page where a practitioner types a password. Added on `/(.*)`: CSP, HSTS (1y), `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin` (which also keeps `/onboard?token=` out of cross-origin `Referer`), and a `Permissions-Policy` denying camera/mic/geolocation/payment/usb.

The CSP allowlist was derived by classifying how each external origin is actually used, not guessed: `unpkg.com` (React, manual-videos) and `esm.sh` (supabase-js, onboard) as scripts; `fonts.googleapis.com`/`fonts.gstatic.com` as styles/fonts; the Supabase project origin + `wss:` for connect. `fda.gov`, `stripe.com` and `apps.apple.com` appear only as `<a href>` — navigation is not CSP-restricted, so they need no entry.

**Honest limitation:** every page inlines `<script>` and `<style>`, so `'unsafe-inline'` is required for both. This CSP constrains *where code loads from*; it does **not** stop inline injection. Real XSS benefit needs the inline blocks moved to files and hashed/nonced — Vercel cannot inject nonces into static files.

**Committed but deliberately NOT pushed** — Vercel auto-deploys `main` and a wrong CSP breaks the live site. Check DevTools for CSP violations on `/`, `/onboard`, `/auth/confirm` and a manual-videos page first. Only `vercel.json` was staged; the repo has 8 unrelated modified files that were left untouched.

### ✅ Correction — `index-genz.html` is NOT deployed

The review flagged it as "reachable, broken form, silently discards signups" (high). **It is not deployed.** The file was never committed and is absent from `origin/main`; Vercel builds from git, so it exists only on the founder's laptop. Same for `og-card.html` and `manual-tile-options.html`. This is the **sixth** finding invalidated by inspecting the real deployed state rather than the working tree — the same class of error as the stale-checkout distortion at the top of this document.

### Dependencies — the two that reach users

Of 52 advisories, almost all are build-time (Expo CLI, Metro, ESLint, Sentry CLI). Exactly two resolve into the shipped bundle, both now pinned via narrow pnpm overrides: `ws` 8.20.0→8.21.3 (via supabase-js → realtime-js) and `nanoid` 3.3.11→3.3.18 (via react-navigation).

Ranges are deliberately narrow — `ws` also resolves at 6.2.3 and 7.5.10 for the Expo and Metro dev servers, and a bare override would drag those to 8.x and break them. Both remain at their original versions. Lockfile diff touches only those two packages (30 lines); `tsc` clean; `pnpm audit` now reports zero runtime-reachable paths for either.

The critical count reads 2→3, which is **not** a regression: the third is a newly-published advisory against `vitest`, a devDependency already on main before this change (it does not appear in the lockfile diff), concerning the Vitest UI server, which this project does not run.

**Not verified:** that the app still builds natively. Changing resolved dependency versions wants an EAS build before shipping, not just a typecheck.

---

## Closeout, 2026-09-08 — items 2, 3 and 4 closed in code

Written on `sec/audit-closeout-2026-09-08`, merged to `main`. **Not deployed** — the
two edge functions need `supabase functions deploy`; nothing else here touches prod.

- **Item 4 (non-constant-time compares) — closed.** `admin-approve-specialist` and
  `admin-compliance-events` now call `isServiceRoleRequest(req, { gatewayVerifiesJwt: true })`.
  Both are pinned `verify_jwt = true` in `config.toml`, so the flag is right; that is
  checkable in review against that file, which is the whole point of the flag being required.
- **Item 3 (429s reach the user as wire noise) — closed.** The raw-fetch path from
  `api/ai.ts` is extracted to `apps/mobile/src/lib/edgeFunction.ts` and the four quota'd
  callers use it: `gear-upc-lookup`, `gear-vision-identify`, `gear-cpsc-check`,
  `milk-vault-scan`. `gear-price-suggest` and `gear-boost-activate` are not in
  `HOURLY_LIMITS` and keep `invoke`. The extracted helper also parses defensively — an
  empty body or an HTML 502 no longer throws a `SyntaxError` that the caller reports as a
  meaningful failure.
- **Item 2 (no edge-function type-check in CI) — closed, pending its first run.**
  The blocker was diagnosed wrong: it is not that `deno check` cannot resolve npm
  specifiers, it is that with no config file under `supabase/functions` Deno walks UP into
  the pnpm workspace, finds the root `package.json` and rewrites it. `supabase/functions/deno.json`
  with `nodeModulesDir: "none"` stops that walk and keeps npm: specifiers in Deno's own
  cache — which is what the deployed runtime does anyway. Workflow:
  `.github/workflows/edge-functions-check.yml`.
  **Run, and the backlog cleared.** Deno was installed and the check executed: it found
  **17 pre-existing errors** across 8 functions, all now fixed. Every one was type-level —
  none was a live defect, and the runtime behaviour of all 8 is unchanged. They fell into
  three groups:

  1. **No generated Database types (9 errors).** The Supabase client is built without a
     `Database` generic, so newer supabase-js resolves an unknown table or RPC name to
     `never` and rejects every column on it. Hit `_shared/rate-limit.ts` (the
     `consume_ai_quota` RPC), `gear-cpsc-check` (the `cpsc_recall_cache` upsert) and
     `playbook-parse-note`. Fixed by describing the row and client shapes actually used
     rather than casting to `any`, which keeps the results checked.
  2. **`PromiseLike` vs `Promise` (3 errors).** `app-help-chat` called `.catch()` on a
     Supabase query builder. Its declared return is `PromiseLike`, which has no `.catch`.
     **Checked before changing it:** `PostgrestBuilder.then` (postgrest-js 2.115.0, line
     367) returns `res.then(onfulfilled, onrejected)` — a real `Promise` — so `.catch()`
     works at runtime and this was never a live bug. Rewritten as two-argument `.then`,
     which is exactly equivalent and type-honest.
  3. **Version drift and lib strictness (5 errors).** `ContentBlock` union narrowing, an
     implicit `any`, a `crypto.subtle.importKey` `BufferSource` mismatch, and
     `cache_control` missing from the resolved SDK's `TextBlockParam`.

  The workflow step was then run verbatim under `sh`: 100 files, exit 0. The final
  invocation deliberately avoids `mapfile` — it needs bash 4+, and macOS ships bash 3.2, so
  a `mapfile` version could not have been verified before reaching CI.

  **Standing caveat:** because the npm specifiers are unpinned (below), this job resolves
  whatever is latest on the day it runs, so a new upstream release can turn it red with no
  change to this repo. Group 3 above is that risk already materialising once.
- **Item 1 (`service_role` rotation) — still open.** Founder task, needs dashboard credentials.

### Specifier pinning — closed 2026-09-08

98 unpinned imports are now pinned: **68** supabase-js to `2.115.0` and **30**
`@anthropic-ai/sdk` to `0.124.0`. Diff is exactly 98 lines changed, 98 removed — pure 1:1
specifier swaps across 72 files, no other edit. `deno check` exit 0 afterwards.

supabase-js was being imported **four different ways** — `jsr:@supabase/supabase-js@2` (31),
`npm:@supabase/supabase-js` (20), `npm:@supabase/supabase-js@2` (6) and
`https://esm.sh/@supabase/supabase-js@2` (11). All four now name the same version. That
mismatch was not theoretical: it is what produced the `SupabaseClient` type-incompatibility
error in `gear-cpsc-check` — two copies of the same library in one program.

**The finding that changed the target version.** The intent was to pin at `2.116.0`, the
latest on npm, jsr and esm.sh. Deno 2.9 refused it: it enforces a **dependency cooldown**,
declining npm/jsr packages published more recently than a cutoff (`2026-09-07 16:06 UTC`) as
supply-chain protection. `esm.sh` served `2.116.0` without complaint, because a URL import
bypasses npm resolution entirely — **so the 11 esm.sh call sites were already pulling a
<24h-old release of supabase-js into production, with no cooldown, no pin, and no review.**
Pinning to `2.115.0` — available everywhere, past the cooldown, and already what the other
57 imports ran — closes that and brings all 68 onto one vetted version.

**Deploy impact checked, not assumed.** `supabase functions deploy` registers an import map
only from a **per-function** `supabase/functions/<name>/deno.json`; the config added for the
CI gate sits one level up at `supabase/functions/deno.json`, so it is never registered and
does not affect bundling. Confirmed by reading the CLI binary's own config resolution, not
by inference from the docs.

**Still floating, deliberately:** `https://esm.sh/@anthropic-ai/sdk@0.27.0` (one call site)
is pinned but very old — moving it is a behaviour change and wants its own commit. And exact
pins close only the DIRECT dependency gap: supabase-js and the Anthropic SDK still resolve
their own transitive deps by range at deploy time. That is why `deno.lock` remains
gitignored — see the note in `.gitignore`.

**Also cleared:** `vitest` was declared in `apps/mobile/package.json` but absent from
`node_modules`, so `pnpm test` could not run and `tsc` reported two module-resolution
errors. A `pnpm install --frozen-lockfile` fixed it with no lockfile change. Suite is green:
43 tests, and `tsc --noEmit` is now clean with zero errors.

## Open — not fixed here, ranked

1. **`service_role` key rotation** (high — founder task, dashboard) — the tooling is done: `specialist-invite.mjs` now reads `supabase/.env.local` first and `.env.example` states where the key belongs. What remains needs credentials: rotate in the dashboard, put the new value in `supabase/.env.local` only, then delete the line from `apps/mobile/.env`. Rotation is required regardless — treat the key as exposed given where it has been living. Safe to do now: the two-mode gate tolerates a key change, which was not true when rotation pain caused authentication to be deleted from six functions.
2. **No type-check for edge functions in CI** (medium, process) — the reason a missing-import defect could reach a commit at all. `deno check` cannot run in this pnpm workspace (it fails to resolve npm specifiers and rewrites `package.json` when it tries), so this needs a Deno-native config for `supabase/functions/` or a resolution shim. Until then the symbol-resolution check must be re-run after any bulk edit.
3. **Per-user rate limiting is server-side only** (low) — `gear.ts` / `milkVault.ts` reach their endpoints via `supabase.functions.invoke`, which does not expose the HTTP status cleanly, so a 429 there still surfaces a generic failure rather than the translated "try again in N min". `api/ai.ts` handles it correctly and is the pattern to copy.
4. **Non-constant-time key compares** (low) — `admin-approve-specialist:27`, `admin-compliance-events:94` use `!==` and do not use the shared helper, so they also carry the key-rotation failure mode that caused authentication to be deleted elsewhere.

### Connect tab — remaining pre-launch gates

Batch 8 closes the message-integrity gate, but the C4/C7 gates recorded in CLAUDE.md still stand and are **not** security findings this audit can close: Postgres GUCs `app.supabase_url` + `app.service_role_key` set so the scan trigger's `pg_net` call actually fires (without them the trigger silently no-ops and messages sit `pending`), at least one active moderator per room, a crisis drill, and the C7 load test. Also note `room-message-scan` remains **fail-open** by design — an Anthropic outage marks messages `clear`. That is a deliberate product decision, but for a self-harm classifier it deserves an explicit re-confirmation before Connect carries real users.

## Unresolvable from the repo — verify in the Supabase dashboard

- Live per-function `verify_jwt` flags. `config.toml` is the intent; the deployed state is authoritative, and past incidents came from exactly that drift.
- Whether `specialist-invite-create`'s deploy flag was flipped when the two-mode gate landed (it passes `gatewayVerifiesJwt: true`, which is only correct if it was).

---

## Verification performed on this branch

- `tsc --noEmit` — clean (exit 0).
- `eslint` on changed files — 0 errors; 17 pre-existing warnings in `ManualScrollV3`.
- Migrations 133 + 134 executed against Postgres 16 with a Supabase-shaped stub (roles `anon`/`authenticated`/`service_role`, default table-wide grants) — both apply clean; privilege-escalation `UPDATE` denied; legitimate profile edit succeeds.
- i18n key parity re-checked after the new strings: 2,135 keys in `en.json` and `es.json`, zero missing either direction.

**Not verified:** migrations have not been applied to the hosted database, and no edge function has been redeployed.
